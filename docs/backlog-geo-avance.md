# Backlog « GEO avancé » — idées R&D (juillet 2026)

Source : `docs/research/2026-07-24-geo-avant-garde.md` (idéation) + `docs/research/2026-07-25-deep-research-citations-verifiee.md` (claims vérifiés 3-0/2-0).
Grille d'organisation : **retrieval → selection → generation** (taxonomie AgentGEO, arXiv 2603.09296, confirmée 2-0).

## Quick wins — section « GEO avancé » ✅ livrés (constaté dans le code le 2026-07-31)

| # | Idée | Principe | Étape | Livré comme |
|---|------|----------|-------|-------------|
| QW1 | 🕐 **Cohérence tripartite de fraîcheur** | `Last-Modified` HTTP vs `dateModified` JSON-LD vs `lastmod` sitemap : s'ils divergent, le signal de fraîcheur est ignoré (anti « fake freshness ») | retrieval | `freshness-coherence` |
| QW2 | 🖋️ **Taux de hedging** | « peut-être / il semble / selon les cas » dans les leads — les moteurs citent les affirmations nettes | selection | `hedging-rate` (advisory, warn max) |
| QW3 | 🎯 **Citabilité par passage** | « unités de réponse » : affirmation directe + chiffre/date/entité + autosuffisante + < N mots | selection | `answer-units` (advisory, warn max) |
| QW4 | ✂️ **Chunk-boundary hygiene** | tableaux sans en-têtes répétés, réponses FAQ séparées de leur question, listes orphelines de leur titre | generation | `chunk-boundary` (advisory, warn max) |

## LOT 5 — socle chunker partagé ✅ livré (2026-07-26, 117 → 119)

Spec : `docs/superpowers/specs/2026-07-26-lot5-chunker.md`.

| Idée | Livré comme | Note |
|------|-------------|------|
| 🧪 **RAG twin / retrieval-sim** | `chunk-retrieval-sim` (4 pts, llm-content) | chunker ~512 tokens dans `checks/chunker.ts` (pur, sans ctx) ; part des fenêtres qui survivent à l'extraction isolée, ancrage compté sur le fil d'intertitres |
| 🔁 **Self-containment anaphorique** | *fusionné* dans `chunk-retrieval-sim` | comme prévu par ce backlog : le test d'ouverture sans renvoi arrière est la moitié du verdict |
| 🧼 **Injection-hygiene** | `injection-hygiene` (3 pts, llm-content) | texte masqué **en ligne** uniquement (pas de CSS récupéré → pas de faux positif `.sr-only`), échec réservé au texte masqué porteur d'instructions modèle, liens UGC sans `rel="ugc"` |

## LOT 6+ (standards à laisser mûrir / coût réseau)

| Idée | Principe | Pourquoi attendre |
|------|----------|-------------------|
| 🤖 **Agent-usability score** | actions clés faisables sans JS (formulaires, labels, schema.org Actions) | ✅ livré — `agent-usability` (`checks/agentic.ts`) |
| 🔁 **Boucle sameAs réelle** | résoudre Wikidata/Wikipedia, matcher libellés, lien retour | ✅ livré — `sameas-verified` |
| ⚔️ **Cohérence d'entité cross-page** | variance des prix/adresses/claims entre pages échantillonnées | précision d'extraction |
| 📇 **Fact-sheet canonique** | page de faits clés machine-lisibles, datés, sourcés (extension `--emit`) | dépend de QW3 |
| 🕳️ **Negative space** | questions évidentes sans réponse par type de site (prix ? horaires ?) | taxonomie par type |
| 🌡️ **Stabilité temporelle** | re-fetch ×2 + diff du contenu principal | double le coût réseau |
| 🔏 **Provenance C2PA** | contenus signés, signal anti-slop | pari 2027 |
| 🗂️ **Matrice politiques IA** | robots.txt × ai.txt × tdmrep.json × RSL : contradictions | pile pas stabilisée |
| 🛰️ **Affordances agentiques** | tier WebMCP / NLWeb / agents.json sur tout le crawl | standards jeunes |

## Ajouts 2026-08-01 — restitution & vérification d'accès (veille SearXNG + signaux internes)

Source : session automatique du 2026-08-01 (recherches SearXNG : guides llms.txt 2026 — seoscore.tools, airanklab.com, limy.ai, pressonify.ai « 11 AI crawlers » ; checklists d'audit IA — loudpixel.ai). Priorisé par ratio impact/effort. Aucun recouvrement avec les lots ci-dessus (qui portent sur de nouveaux *checks de contenu* ; ici : validation d'artefacts existants + ergonomie de restitution).

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A1 | ✅ 🧾 **Lint d'un llms.txt existant** — livré 2026-08-01 (check `llms-txt-lint`, commit `e94a041`) | on sait *générer* un squelette (`--emit`), mais pas *valider* un llms.txt déjà en place : format (H1 unique, blockquote, sections H2, liens Markdown), liens qui résolvent (pas de 404), cohérence avec le sitemap et le contenu réel. Les guides 2026 en font un standard d'audit à part entière | fort / faible |
| A2 | 🚪 **Accès effectif des crawlers IA** | au-delà de robots.txt (`ai-access`) : requêter le site avec les User-Agents des crawlers IA majeurs (GPTBot, ClaudeBot, PerplexityBot…) et comparer les codes/contenus obtenus, pour détecter les blocages CDN/WAF (Cloudflare & co) invisibles dans robots.txt | fort / moyen |
| A3 | ✅ 🥇 **« Top 3 corrections » en tête de rapport** — livré 2026-08-01 (payoff = points pondérés ÷ effort par famille, terminal + HTML, commit `f8f90f3`) | trier les échecs par points récupérables ÷ effort estimé et les afficher en tête (terminal + HTML) ; différenciant vs les audits « liste plate » | moyen / faible |
| A4 | 📈 **Sparkline de score dans le rapport HTML** — ✅ livré le 2026-08-01 (section « Score dans le temps » : sparklines SVG inline global + par famille dès 2 runs, i18n EN/FR, 15 tests) | `--history` stocke déjà la série ; l'afficher (tendance du score global + par axe) dans le HTML | moyen / faible |
| A5 | 💬 **Commentaire de PR via l'action GitHub** | `action.yml` existe ; publier le diff vs `--baseline` (checks gagnés/perdus, delta de score) en commentaire de PR, comme les outils CI comparables | moyen / moyen |

## Garde-fous (recherche vérifiée)

- Ne jamais promettre « +40 % » (réfuté 0-3) ; l'efficacité varie par domaine (confirmé 3-0).
- La retrouvabilité bat la réécriture stylistique (confirmé 3-0) — les checks amont restent prioritaires.
- Une part des réponses IA se fait sans fetch live ni citation (34 %/24 %, 92 % sans lien pour Gemini) : vendre la *probabilité* de citation, jamais la garantie.

## Ajouts 2026-08-01 — 2e session (hygiène de dépôt + veille SearXNG audit technique)

Sources : signaux internes (fichiers de travail à la racine du dépôt, chantier local non commité `packages/cli/src/report/history.ts` — probable A4 en cours) ; SearXNG local (seomix.fr audit technique, lafabriquedunet.fr alternatives Screaming Frog : sitemaps/canonical/pagination, oplia.fr preuve sociale). Sans doublon avec A1-A5.

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A6 | ✅ 2026-08-02 **Nettoyer la racine du dépôt** | déplacer/ignorer les fichiers de travail (`extract_lantern.js`, `final_quotes.js`, `pull_strings.js`, `quote_windows.js`, `lantern_*.txt`, `audit-prod.json`/`.err`, `findable.bordebat.fr-baseline.json`) vers `examples/` ou `.gitignore` ; dette qui pollue clones et recherches | moyen / faible |
| A7 | 🗺️ **Croisement sitemap.xml vs crawl** | lire `sitemap.xml` (découverte via robots.txt) et comparer aux pages atteintes par liens internes : pages orphelines et pages crawlé-mais-absentes-du-sitemap ; check standard des crawlers type Screaming Frog, borné par `--max-pages` | fort / moyen |
| A8 | ✅ 2026-08-26 (livré : check `social-proof`, i18n FR/EN, guides EN+FR, suite 1453 tests verte) ⭐ **Check preuve sociale structurée** | détecter la présence d'avis/notes balisés (`AggregateRating`, `Review` JSON-LD) sur les pages clés des sites locaux ; argument de vente direct pour la prospection PB OpenTech (les fiches + avis convertissent) | moyen / faible |
| A9 | 📌 **Terminer et committer le chantier sparkline (A4)** — ✅ livré le 2026-08-01 (chantier finalisé, 15 tests ajoutés `test/report/history.test.ts`, suite 1335 PASS, commité/poussé) | `history.ts` + modifs `html.ts`/`i18n.ts`/`index.ts` existent en local non commité : finaliser, tester, committer — sinon risque de perte et de dérive avec `origin/main` | fort / faible |

## Ajouts 2026-08-02 (session automatique — signaux internes + veille SearXNG)

Signaux internes : 1573 tests verts (1335 CLI + 238 web) mais les 238 tests web ne tournent jamais en CI (`apps` absent des workspaces racine) ; le catalogue i18n compte **138 checks** alors que README/guides annoncent 137 (`llms-txt-lint` non documenté) ; CHANGELOG figé à 0.10.0 malgré 3 features livrées depuis (e94a041, f8f90f3, 700b14a) ; CLI muet pendant un crawl (callback `onProgress` de `runner.ts:89` utilisé uniquement par le web) et messages de succès émis sur stderr (`index.ts:352,371,388` — d'où le faux `audit-prod.err`). Sources externes : comparatifs GEO 2026 (seranking.com, vegavid.com, webconversion.fr — les suites GEO vendent le suivi multi-moteurs IA + audit sémantique), abondance.com « Query fan-out : 102 000 requêtes analysées », docs.github.com (upload SARIF), github.com/topics/seo-audit (le concurrent CLI de référence affiche « 108 rules across 12 categories » — notre différenciation = 138 checks, à condition que la doc soit exacte). Sans doublon avec A2/A5/A6/A7/A8 ni LOT 6+.

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A10 | ✅ 2026-08-02 **Progression CLI + flux de sortie propres** | brancher `onProgress` (déjà exposé par `runner.ts:89`) sur un affichage « page 3/10 » côté CLI ; basculer les messages de succès sur stdout (ou `--quiet`) et ajouter `--no-color`/respect de `NO_COLOR` — un audit de 15-30 s totalement muet et un stderr non vide en succès sont les deux plus gros irritants d'usage | fort / faible |
| A11 | ✅ 2026-08-02 📦 **Release 0.11.0 + docs des checks exactes** | bump version, section CHANGELOG pour les 3 features post-0.10.0, réaligner 137→138 partout (README ×2, guide.md, guide.fr.md), documenter `llms-txt-lint` dans les deux guides, et ajouter un test qui verrouille « nombre de checks == nombre de sections du guide » (sur le modèle du test des flags) | fort / faible |
| A12 | ✅ 2026-08-02 **CI complète : tests web + build dist** | ajouter `apps` aux workspaces (ou script `test:web` appelé par la CI) pour exécuter les 238 tests web, et garantir un build du `dist` CLI avant le démarrage du serveur web (9 imports de `apps/web/server.mjs` pointent vers `../../packages/cli/dist/`) — aujourd'hui un dist périmé casse le web sans qu'aucun test ne le voie | fort / faible |
| A13 | 🌀 **Check « couverture query fan-out »** | évaluer si une page répond aux sous-questions que les moteurs IA génèrent autour de la requête principale (variantes comparatives, prix, « pour qui », localisation) — l'analyse abondance.com de 102 000 requêtes montre que ChatGPT décompose systématiquement ; complète `answer-units`/`chunk-retrieval-sim` sans les recouvrir | fort / moyen |
| A14 | 🏗️ **Découpage `apps/web/server.mjs` + montée node-html-parser** | 2085 lignes (routage + vues + SSE + admin + OG-image) alors que le pattern d'extraction existe déjà dans `apps/web/lib/` (12 modules) ; planifier la montée `node-html-parser` 6.1.13 → 9.0.1 (dépendance runtime du parsing, 3 majeures de retard) | moyen / moyen |

## Ajouts 2026-08-02 (3e session automatique — recherche d'améliorations)

Signaux internes : A10/A11/A12 livrés aujourd'hui (0.11.0, CI web, CLI progress) ; le CLI ne sait se comparer qu'à son propre passé (`--baseline`/`--history`), jamais à un tiers ; le rapport HTML reste un rapport d'expert, inutilisable tel quel en prospection TPE ; tous les checks sont statiques (aucune mesure du crawl IA réel). Sources externes (SearXNG) : les suites GEO 2026 vendent le benchmark concurrentiel multi-sites (seopital.co, stafe.fr) ; theweblead.fr — l'audit gratuit est l'accroche n°1 en agence ; shine.fr/plateya.fr — le référencement local est le levier décisif des artisans. Sans doublon avec A13/A14 ni LOT 6+ (la « cohérence d'entité cross-page » reste distincte de A18).

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A15 | ⚔️ **Mode benchmark concurrentiel** | `findable-audit compare <url> <concurrent1> [concurrent2]` : même jeu de checks sur chaque site, tableau d'écarts par axe + « ce que le concurrent fait et pas vous » — le comparatif multi-sites est l'argument central des suites GEO 2026, et le runner sait déjà tout faire, il manque l'orchestration et le rendu | fort / moyen |
| A16 | 📄 **Rapport prospect one-page white-label** | flag `--report prospect` : une page HTML imprimable (score, 3 forces, 3 corrections chiffrées en € /jours, zéro jargon, logo personnalisable) — transforme l'outil en machine à leads pour pb-opentech et pour tout indépendant ; l'audit gratuit est l'accroche n°1 de la prospection TPE | fort / faible |
| A17 | 🤖 **Analyse des logs serveur : crawl IA réel** | sous-commande `logs <access.log>` : fréquence et pages visitées par GPTBot/ClaudeBot/PerplexityBot/Google-Extended, croisées avec robots.txt et le sitemap — répond à « les IA me lisent-elles vraiment ? », seule mesure factuelle que les checks statiques ne peuvent pas donner | fort / moyen |
| A18 | 📍 **Checks « profil local » (cible TPE/artisans)** | présence et complétude de `LocalBusiness` (NAP, horaires, `geo`, zone d'intervention, lien `hasMap`/GBP dans `sameAs`) + page contact actionnable sans JS — l'axe décisif pour la clientèle visée, aujourd'hui sous-couvert ; distinct de la variance cross-page du LOT 6+ | moyen / faible |

## Ajouts 2026-08-03 (session automatique — signaux internes + veille SearXNG)

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A19 | ✅ 2026-08-03 (livré : check `ai-crawler-reachability`, 139 checks, tests verts) 🚪 **Vérification d'accès crawlers IA en conditions réelles** | requêtes HTTP effectives sur les pages clés avec les User-Agent GPTBot/ClaudeBot/PerplexityBot/Google-Extended et comparaison des statuts/contenus avec la requête « navigateur » — les WAF/CDN bloquent souvent les bots IA silencieusement alors que robots.txt les autorise ; complète les checks statiques (robots.txt, llms.txt) par la seule preuve empirique, distinct de A17 (qui lit les logs, ici on teste activement) | fort / faible |
| A20 | ✅ 2026-08-03 (livré : `action.yml` composite testée, doc README ; publication npm volontairement exclue en session auto) 📦 **Distribution : paquet npm + GitHub Action officielle** | publier le CLI sur npm et fournir une action `findable-audit-action` prête à l'emploi (audit sur PR, `--baseline` + `--fail-on-regression` déjà en place) — les outils d'audit 2026 gagnent leur adoption dans les pipelines CI (modèle Lighthouse CI) ; l'effort est quasi nul, le moteur existe, il manque l'emballage | moyen / faible |
| A21 | 🗣️ **Couche de recommandations rédigées (LLM local) dans le rapport** | après le scoring factuel, passer les échecs au LLM local pour générer des recommandations priorisées en langage client (quoi, pourquoi, gain estimé), à la manière des audits « AI-grounded » type ai-website-audit-cli — nourrit directement le one-page prospect (A16) sans dénaturer le cœur déterministe ; les métriques restent la source de vérité, le LLM ne fait que rédiger | moyen / moyen |

## Ajouts 2026-08-06 (session automatique — signaux internes + veille SearXNG)

Signaux internes : la comparaison temporelle existe (`--baseline`/`--history`/`--fail-on-regression`) mais reste passive — aucun mode planifié ni alerte ; le CLI lint `llms.txt` (139 checks) mais ne sait pas le produire ; les données d'audit meurent dans leur JSON au lieu de nourrir vigie-seo. Sources externes (SearXNG) : visibletoai.dev (audit « AI readability », couple `llms.txt`/`llms-full.txt`), gen-optima.com et evertune.ai (les plateformes GEO 2026 vendent le monitoring continu avec alertes, pas l'audit ponctuel), geochecker.com.tr (concurrent gratuit GEO Checker : positionnement mono-scan, notre différenciation = suivi). Sans doublon avec A1-A21.

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A22 | ✅ 2026-08-06 **Génération `llms.txt`/`llms-full.txt` depuis le crawl** — livré : sous-commande `findable generate llms-txt <url> [--out <dir>]` (crawl seul, pas d'audit ni de rapport), fichiers construits depuis les vraies pages, doc README + USAGE, 4 tests CLI | sous-commande `generate llms-txt` : produire un couple `llms.txt` (index) / `llms-full.txt` (contenu) cohérent avec le sitemap et les pages auditées — on passe du diagnostic (`llms-txt-lint`) à la remédiation en un geste ; forte valeur perçue, le crawl a déjà toutes les données | fort / faible |
| A23 | ⏰ **Mode monitor : audit planifié + alertes** | `findable-audit monitor <url>` : ré-audit périodique, diff vs baseline (mécanique `--fail-on-regression` déjà en place) et notification (webhook/e-mail/fichier) en cas de régression — les suites GEO 2026 se vendent sur le monitoring continu, pas le scan ponctuel ; c'est l'emballage qui manque, pas le moteur | fort / moyen |
| A24 | 🌉 **Pont findable-audit → vigie-seo** | export normalisé des scores par axe vers la base vigie-seo pour historiser l'audit multi-sites dans le dashboard existant (courbes, alertes, rapport client V25) — évite de re-développer une UI de suivi côté findable-audit et soude la gamme produit | moyen / moyen |

## Ajouts 2026-08-07 (session automatique — signaux internes + veille SearXNG)
- A25 — ◐ 2026-08-08 (partiel : warn dédié Google-Extended livré le 2026-08-07 ; traduction FR i18n du message livrée le 2026-08-08, tests verts ; reste : exiger les données structurées recommandées par le guide) Aligner le scoring sur le guide officiel Google « AI features » (developers.google.com/search/docs/appearance/ai-features) : vérifier qu'aucun blocage involontaire de Google-Extended n'empêche l'éligibilité AI Overviews, et exiger les données structurées qu'il recommande. | Impact fort / effort moyen
- A26 — Check « citabilité ChatGPT » : fraîcheur des dates, auteur identifiable, statistiques sourçables — critères de sélection des sources documentés en 2026 (referencement-llm.com). | Impact moyen / effort moyen
- A27 — ✅ 2026-08-07 Commande generate : remplacer les placeholders TODO du llms.txt généré par du contenu dérivé du crawl (titres/descriptions réels) — generate.test.ts:158 accepte encore /todo|paste/, signe de sortie inachevée. | Impact fort / effort faible
- A28 — Benchmark sectoriel anonymisé : agréger les scores des audits stockés côté web (store) pour afficher un percentile « votre site vs sites comparables » dans le rapport. | Impact moyen / effort moyen
- A29 — ✅ 2026-08-07 Élargir SOCIAL_RE (packages/cli/src/checks/llm-content.ts:392) : bsky.app, discord.gg/.com, whatsapp.com, twitch.tv manquent — faux signaux possibles sur les liens sociaux. | Impact faible / effort faible

## Ajouts 2026-08-07 (session automatique, 2e passe — signaux internes + veille SearXNG)

- A30 — ✅ 2026-08-07 Avertissement Cloudflare 15/09/2026 dans `ai-access` : Cloudflare bloquera par défaut les crawlers IA à partir du 15 septembre 2026 (blog.cloudflare.com, nooki.fr). Détecter si le site audité est servi par Cloudflare (en-têtes `cf-ray`/`server`) et, le cas échéant, émettre un avertissement daté + recommandation de vérifier AI Crawl Control. Impact : fort (échéance imminente touchant beaucoup de sites). Effort : faible. Priorité : haute.
- A31 — ✅ 2026-08-07 Check cohérence NAP / entité locale : comparer nom, adresse et téléphone entre JSON-LD `LocalBusiness`, footer et page contact. La dissonance NAP est identifiée comme signal négatif SEO local + GEO (noiise.com 12/2025, oplia.fr 07/2026). S'appuie sur les checks structured-data existants ; complète A26 (citabilité) pour la cible artisans/TPE. Impact : fort sur la cible. Effort : moyen.
- A32 — ✅ 2026-08-07 Résumé exécutif + « top 5 quick wins » triés impact/effort en tête de rapport (CLI et HTML) : tri déterministe des échecs par sévérité × coût de correction estimé par check — distinct de A21 (recommandations LLM). Les audits GEO commerciaux (ex. detekia.fr, 9 étapes + checklist priorisée) vendent d'abord cette hiérarchisation. Impact : moyen-fort (lisibilité client). Effort : faible.

## Ajouts 2026-08-08 (session automatique — signaux internes + veille SearXNG)

| # | Idée | Principe | Impact / Effort |
|---|---|---|---|
| A33 | ✅ 2026-08-08 (livré : warn « signal non contraignant » dans ai-access.ts, i18n FR incluse, commit 304e2f7, tests verts) **Nuance « Content Signals » dans le check Cloudflare** | Google ignore les directives Content Signals ajoutées au robots.txt (John Mueller, relayé positionzero.net 07/07/2026) ; compléter le check cloudflare-ai-defaults (A30) : ne pas présenter Content Signals comme une protection effective, message dédié « signal non contraignant » | Impact : conseil exact, évite un faux sentiment de contrôle chez le client. Effort : faible |
| A34 | ✅ 2026-08-09 (livré : pondération réduite du check llms-content + phrase de transparence FR/EN dans le README généré, commit dbd767f, 238 tests verts) **Messaging honnête sur llms.txt** | adoption contestée en 2026 (« no AI system currently uses llms.txt », débat relayé ellevate.fr 28/04/2026 ; limites détaillées developr.fr 06/05/2026) : repositionner le check + la génération comme « coût quasi nul, bénéfice incertain », pondération réduite dans le score et phrase de transparence dans le rapport | Impact : crédibilité de l'outil face à un client informé. Effort : faible |
| A35 | 📅 **Mode audit récurrent + rapport de tendance** | pratique AEO 2026 : « geler et rejouer » l'audit à intervalle fixe (ailabsaudit.com) ; réutiliser --baseline/--history existants pour produire un rapport de tendance (delta score, checks régressés/gagnés) prêt à envoyer au client + doc d'ordonnancement | Impact : transforme l'audit one-shot en suivi vendable en récurrence. Effort : moyen |

## Ajouts 2026-08-09 (session automatique — recherche d'améliorations)

Signaux internes : aucun TODO/FIXME dans `packages/` ; `report/axes.ts` n'expose aucun axe confiance/autorité alors que les signaux (auteur/dates A26, `sameas-verified`, liens sortants) sont déjà mesurés ; la sortie de rapport reste technique, inutilisable telle quelle en prospection TPE. Sources externes (SearXNG) : genee.tech (audit GEO gratuit comme outil commercial), e-cybercom.fr 11/04/2026 (« le balisage Schema.org est la traduction machine-readable de votre E-E-A-T »), edikka.com 16/06/2026 (site agent-ready), universalcommerceprotocol.fr (catalogue lisible par agents), yvarn.fr 13/05/2026 (12 leviers GEO). Sans doublon : benchmark concurrentiel = A15, citabilité/auteur/dates = A26, NAP local = A31/A18, formulaires agent = LOT 6 (`agentic.ts`), FAQ/chunks = QW4 + `llm-content.ts`, tendance = A35.

| # | Idée | Principe | Impact/Effort |
|---|------|----------|---------------|
| A36 | 📄 **Sortie « one-pager » commerciale (FR, non technique)** | `--format onepager` : 1 page (score global, 5 actions prioritaires en langage client, bénéfice attendu) prête à joindre à une prospection écrite — pratique 2026 des agences (« audit gratuit » genee.tech) ; synergie directe avec P33/P35 de pb-opentech | fort / moyen |
| A37 ✅ 2026-08-09 | 🎓 **Axe de lecture E-E-A-T agrégé dans le rapport** | regrouper les signaux déjà calculés (auteur identifiable, fraîcheur des dates, `sameAs` vérifiés, sources sortantes) en un axe « confiance/autorité » avec recommandations — aujourd'hui absent de `report/axes.ts` alors que tout est mesuré ; e-cybercom 11/04/2026 | moyen / faible |
| A38 ✅ 2026-08-09 | 🛰️ **Veille + check optionnel des standards d'actionnabilité agent** | détecter `agents.json` / signaux UCP derrière un flag expérimental, en warning purement informatif (philosophie du repo : « no unstable standard » en check noté) — universalcommerceprotocol.fr, edikka.com | faible / faible |

## Ajouts 2026-08-09 (session automatique — veille externe)
- [x] A39 (fait 2026-08-09 - docs/research/2026-08-09-benchmark-checkers-2026.md ; aucun manque identifie) — Benchmark de couverture vs checkers gratuits 2026 : Siftly Crawler Audit (7 zones : HTTPS, robots, headers, meta, sitemap, SSR, structured data), PageForge AI Search Readiness, Search Engine Land AI Agent Readiness Checker, Apify AI Readiness Auditor. Matrice checks couverts/absents chez nous -> prioriser les manques + alimenter l'argumentaire « pourquoi payer un audit complet ». Aucun de ces noms n'apparait dans le backlog actuel. | Impact moyen / effort faible
- [x] A40 (fait 2026-08-09 - check `pay-per-crawl` dans packages/cli/src/checks/ai-access.ts, i18n fr/en, 6 tests) — (R&D, opportuniste) Check « monetisation IA » : detecter la config pay-per-crawl (Cloudflare micro-paiements bots, en-tetes 402 Payment Required) et signaler l'opportunite/le risque (bloque les citations si active par defaut). Signal emergent veille 09/08 : metadonnees 402 pressenties dans llms.txt en 2026. | Impact faible / effort faible

## Ajouts 2026-08-10 (session automatique — recherche d'améliorations)
- [ ] A120 [2026-08-10] **Séparer `test` du build dans le monorepo** — le script racine `test` enchaîne `npm run build --workspaces` avant chaque exécution : boucle de dev lente et CI plus coûteuse. Ajouter `test:fast` (tests seuls) et ne rebuilder que si dist absent. Impact moyen / effort faible.
- [ ] A121 [2026-08-10] **Score agrégé par gabarit de page** — en mode crawl multi-pages, regrouper les URLs par template (accueil / fiche / article) et produire un score pondéré par gabarit, façon unlighthouse (unlighthouse.dev), plutôt qu'une seule liste plate par URL. Impact moyen / effort moyen.
- [ ] A122 [2026-08-10] **Audit comparatif de domaines tiers** — réutiliser la mécanique --baseline/--compare pour auditer 1-2 concurrents d'un client et sortir un delta de findabilité chiffré ; argument commercial direct pour la prospection TPE-PME. Impact fort / effort moyen.

## Ajouts 2026-08-15 (session automatique — recherche d'améliorations)
- [ ] A123 [2026-08-15] **Lentille « answer-readiness » par page** — scorer la citabilité des réponses elles-mêmes : bloc réponse autonome (question → réponse directe 40-60 mots), statistiques datées, FAQ balisée ; les outils AEO 2026 (comparatif HubSpot 07/2026) mesurent cela au-delà des prérequis techniques. Vérifier le recouvrement avec la lentille E-E-A-T (A37) avant d'implémenter. Impact fort / effort moyen.
- [ ] A124 [2026-08-15] **Mode `--logs` : analyse de logs serveur crawlers IA** — parser un access.log fourni par le client : part de hits GPTBot / ClaudeBot / PerplexityBot / Google-Extended, vérification reverse-DNS anti-faux-bots, delta avec ce que robots.txt autorise ; les hits crawlers IA n'apparaissent pas dans les analytics JS, seule l'analyse de logs prouve « les IA me lisent ». Différenciant vs checkers gratuits (aucun ne le fait, cf. matrice A39). Impact fort / effort moyen.
- [ ] A125 [2026-08-15] **Export one-pager « top 5 actions »** — synthèse décision d'une page (HTML/PDF) priorisée impact/effort à partir des 142 checks, format vendable TPE ; les outils GEO commerciaux 2026 vendent la recommandation actionnable, pas la liste exhaustive. Impact fort / effort faible-moyen.
- [ ] A126 [2026-08-15] **Plan de distribution : figurer dans les comparatifs d'outils GEO 2026** — vigie-seo mesure un indice de visibilité 0 et 0 citation IA pour findable-audit ; préparer un dossier de soumission aux listicles FR (Digitiz, Plateya, Facilitateur Numérique, Google AIO SEO…) + page « alternatives » s'appuyant sur la matrice benchmark A39 déjà rédigée. Impact fort / effort faible.

## Ajouts 2026-08-15 (session automatique, 2e passe — croisement signaux internes + veille SearXNG)
- A41 [2026-08-15] **Vue « score par moteur »** — matrice checks × moteurs (ChatGPT/GPTBot, Claude/ClaudeBot, Perplexity, Gemini/Google-Extended) : chaque check n'impacte pas les mêmes moteurs ; métadonnée `engines[]` par check + sous-scores par moteur dans le rapport. Tous les comparatifs GEO 2026 (seo.fr, aioseo.fr 14/07/2026, tool-advisor.fr 30/07/2026) segmentent par moteur. Impact fort / effort moyen.
- A42 [2026-08-15] **Check « contenu visible sans JavaScript »** — comparer le texte utile du HTML brut à celui du DOM rendu : la plupart des crawlers LLM n'exécutent pas le JS ; alerter si une part significative du contenu n'apparaît qu'après rendu client. Aucune couverture actuelle (0 mention JS/SSR côté checks llm-content). Impact fort / effort moyen (le crawler fait déjà le fetch brut ; ajouter une passe DOM optionnelle).
- A43 [2026-08-15] **Profils sectoriels de scoring (`--profile local|ecommerce|docs`)** — pondérations prédéfinies par cas d'usage : un site vitrine d'artisan local (LocalBusiness, NAP, GBP) n'a pas les priorités d'une doc SaaS (llms-full.txt, ancres stables) ; ajuste poids et seuils sans toucher aux checks. Impact moyen / effort moyen.

## Ajouts session automatique 2026-08-16 (phase recherche-ameliorations)

- A44 [2026-08-16] **`findable recheck` ciblé post-correction** — re-exécuter uniquement les checks en échec du dernier rapport (lecture du `--baseline`, option `--checks id1,id2`) pour une boucle corrective rapide chez le client, sans repayer un audit complet (crawl + 139 checks). Complète `--fail-on-regression` (A?) et le futur mode monitor (A23) sans les recouvrir. Impact moyen / effort faible-moyen.
- A45 [2026-08-16] **Profil sectoriel SaaS B2B (extension de A43)** — A43 prévoit `--profile local|ecommerce|docs` ; les audits GEO B2B SaaS 2026 (score-geo.fr) pondèrent `SoftwareApplication` en JSON-LD, la transparence pricing et le contenu comparateur. Ajouter `--profile saas` avec ces pondérations. Impact moyen / effort faible (une fois A43 livré).
- [x] A46 [2026-08-16, livré 2026-08-17] **Doc de positionnement face aux suites GEO 2026** — Semrush a absorbé AI Visibility Toolkit dans Semrush One (digitiz.fr 28/07/2026) ; mentionlab.ai distingue explicitement « technical GEO audit » (plus profond, schema, SSR) du monitoring de mentions. Rédiger un tableau honnête README/site « audit technique profond (findable-audit) vs monitoring de visibilité (Profound, Semrush One, vigie-seo pour la mesure locale) » + quand utiliser quoi. Impact moyen (commercial) / effort faible. **Note** : le tableau comparatif honnête existait déjà (section « How it compares (honestly) », README.md) et couvrait déjà la distinction audit/monitoring dans les bullets « Where alternatives beat us today ». Complété : nom à jour « Semrush One » (absorption AI Visibility Toolkit), et paragraphe dédié qui explicite la distinction input-side (audit ponctuel, aucun compte) vs output-side (monitoring récurrent avec compte) — sans lien vers vigie-seo (outil interne non public). Pas de section équivalente dans les guides FR/EN (le tableau est README-only), donc aucune duplication nécessaire.
- A47 [2026-08-16] **Couverture query fan-out au niveau site (extension de A13)** — A13 évalue par page ; les comparatifs 2026 jugent les outils sur « Prompt Volumes » et query fan-out (aioseo.fr 14/07/2026). Générer depuis le crawl la liste des questions probables du domaine, puis lister les « questions sans page/chunk de réponse » (gap analysis multi-pages) dans le rapport. Impact fort / effort moyen.

## Ajouts session automatique 2026-08-17 (phase recherche-ameliorations)

Signaux internes : aucun TODO/FIXME nouveau, monorepo sain. L'item « answer-readiness par page » ajoute le 2026-08-15 (sans ID) reste ouvert et se recoupe avec les pistes ci-dessous — a fusionner en implementation plutot que redoubler. Veille SearXNG (plateya.fr comparatif 25 outils 2026) : Sitebulb convertit les constats techniques en graphe visuel urgence/impact pour des interlocuteurs non techniques ; Semji/Thot SEO scorent la « citabilite » du contenu sur des criteres precis (structure factuelle, sources verifiables, reponse directe) plutot que des heuristiques globales.

- [LIVRE 2026-08-17] A48 **Graphe visuel priorite impact/urgence dans le rapport HTML** — A32 (livre) trie deja les quick wins par impact x effort mais uniquement en liste ; ajouter une visualisation scatter (dataviz SVG deja en place ailleurs dans le rapport) impact vs effort pour les checks en echec, pattern Sitebulb repris par les comparatifs 2026 — rend le rapport plus vendable a un interlocuteur non technique (echo direct de A36 one-pager). Impact moyen-fort (lisibilite client) / effort faible (reutilise l'infra dataviz existante). Implemente : `renderImpactEffortScatter` (beeswarm SVG deterministe) dans `charts.ts`, i18n FR/EN dans `i18n.ts`, section injectee dans le plan d'action de `html.ts`. Suite complete verte (89 fichiers / 1394 tests CLI + 238 tests web).
- A49 [2026-08-17] **Score de citabilite structurelle par page (reponse directe + source datee + verifiable)** — extension concrete de l'item « answer-readiness » deja note le 2026-08-15 : mesurer precisement (1) presence d'une reponse directe dans les 40-60 premiers mots apres un titre-question, (2) presence d'au moins une statistique ou affirmation datee, (3) presence d'une source/lien sortant verifiable a proximite — 3 sous-scores objectifs plutot qu'un score agrege flou, inspires du modele Semji/Thot SEO (plateya.fr 2026). Verifier le recouvrement avec la lentille E-E-A-T (A37) avant implementation. Impact fort / effort moyen.

## Ajouts session automatique 2026-08-18 (phase recherche-ameliorations)

Signaux internes : monorepo sain (aucun TODO/FIXME dans `packages/*/src/`), famille de checks `accessibility.ts` existante mais sans reference a une echeance legale precise. Veille externe (WebSearch, 08/2026) : depuis l'European Accessibility Act (juin 2025), le RGAA v4.1 (106 criteres) s'applique desormais aux entreprises privees de plus de 10 salaries, avec des sanctions jusqu'a 50 000 EUR par service non conforme renouvelables tous les 6 mois (webconforme.fr, etcdigital.fr, gnth.fr) ; l'etude WebAIM 2026 releve 95,9 % des pages non conformes ; les outils gratuits (axe DevTools, WAVE, Lighthouse, RGAA Checker) ne couvrent que 30-40 % des criteres RGAA, laissant une place claire a un audit automatise plus complet sur les criteres testables sans humain. RGAA 5 est annonce en preparation. Sans doublon : famille accessibility existante = checks structurels deja mesures (contraste, alt, landmarks, etc. — a verifier item par item avant implementation), Cloudflare AI defaults deadline = A30 (meme pattern « avertissement date »).

- [x] A50 [2026-08-18, livre] **Avertissement legal date « RGAA / European Accessibility Act » dans la famille accessibility** — meme pattern que A30 (avertissement Cloudflare AI defaults) : quand le score de la famille `accessibility` est faible, ajouter un encart date rappelant que les entreprises >10 salaries sont depuis juin 2025 soumises au RGAA v4.1 (amendes jusqu'a 50 000 EUR/service), avec un lien vers l'echeance RGAA 5 a surveiller — transforme un score technique abstrait en argument commercial urgent, sans changer la ponderation des checks existants. Impact fort (echeance legale concrete pour la cible TPE/PME) / effort faible. Implemente : nouveau check `rgaa-eaa-deadline` (`checks/accessibility.ts`, famille `accessibility`, 2 pts, 143e checks total), warn date des le premier defaut structurel detecte (lang/landmarks/alt), pass/skip sinon (invariant perfect-site=100 preserve). i18n EN/FR (`check-i18n.ts`, `message-i18n.ts`), doc guide.md/guide.fr.md + compteurs README a jour. Suite complete verte (1398 tests CLI).
- A51 — **Verifier la couverture des checks accessibility vs les criteres RGAA 4.1 les plus cites (contraste, alt-text, focus clavier, landmarks ARIA)** — l'etude WebAIM 2026 (56 erreurs/page en moyenne) et les guides RGAA 2026 pointent un socle recurrent de criteres en echec ; auditer `checks/accessibility.ts` face a ce socle et combler les lacunes testables automatiquement (les 30-40 % couverts par les outils gratuits generalistes type axe/WAVE/Lighthouse) sans dupliquer un audit humain complet. Impact moyen-fort / effort moyen.
- [x] A52 [2026-08-18, livre] **Lien explicite accessibilite <-> Core Web Vitals dans le rapport** — la veille 2026 souligne que 70 % des exigences techniques accessibilite/SEO se recoupent (utilisateurs handicapes sur reseaux/equipements plus lents -> les optimisations perf les servent en premier) ; croiser dans le plan d'action les checks `accessibility` en echec avec les checks `performance`/CWV correles (ex. temps de chargement, poids page) pour presenter un seul chantier au lieu de deux sections disjointes. Impact moyen / effort faible (reutilise des donnees deja mesurees par les 2 familles). Implemente : nouveau message i18n `a11yPerfLink` (`report/i18n.ts`, EN/FR), note inseree dans la section accessibility (HTML `report/html.ts` + Markdown `report/markdown.ts`) uniquement quand les deux familles ont des checks en echec/warn — aucune nouvelle sonde, reutilise `report.results` deja note. 5 tests ajoutes (html.test.ts, markdown.test.ts). Suite complete verte (1405 tests CLI + 238 web).

## Ajouts session automatique 2026-08-18, 2e passe (phase recherche-ameliorations)

Signaux internes : aucun check ne mentionne `Google-Agent` ni de metrique de ratio crawl/referral. Veille externe (WebSearch 08/2026, brandlume.com, ultrascout.ai, searchenginejournal.com) : Google a ajoute `Google-Agent` a sa liste officielle de fetchers le 20/03/2026 — a la difference des crawlers d'indexation, il agit pour le compte d'un utilisateur et **ignore robots.txt** (le bloquer necessite une authentification serveur, pas une directive robots.txt) ; Cloudflare Radar chiffre un ratio crawl/referral tres defavorable pour les bots IA (ClaudeBot ~20 600 pages crawlees pour 1 referral, OpenAI ~1 300:1), donnee absente de nos rapports alors qu'elle objective le cout reel de l'ouverture aux bots IA. Sans doublon : Content Signals = A33, pay-per-crawl = A40, roster 28 bots = LOT 3.

- [x] A53 [2026-08-18, livre] **Detecter et documenter `Google-Agent` distinctement des crawlers d'indexation classiques** — dans le check ai-access (roster bots, LOT 3), ajouter `Google-Agent` avec une note specifique : robots.txt ne le bloque pas (agent utilisateur, pas crawler autonome), seul un controle cote serveur (auth, rate-limit applicatif) fonctionne — evite au client de croire a tort qu'un `Disallow` le protege. Impact moyen (exactitude du conseil) / effort faible. Implemente : `Google-Agent` ajoute a `CITATION_BOTS` (`robots.ts`, roster 28 -> 29), callout dedie dans `aiCrawlersAllowed` (`checks/ai-access.ts`) quand il est bloque (fail + message/fix specifiques, ignore la regle generique), i18n FR (`message-i18n.ts`, `check-i18n.ts`), compteurs README/guide.fr.md a jour (28 -> 29). 2 tests ajoutes. Suite complete verte (1400 tests CLI + 238 web).
- [x] A54 [2026-08-18, livre] **Metrique contextuelle « ratio crawl/referral » par bot dans le rapport** — afficher a titre informatif (non note, pas de poids sur le score) les ratios publics Cloudflare Radar par bot IA majeur (ClaudeBot, GPTBot, PerplexityBot) a cote de la liste des acces autorises/bloques — aide le client a arbitrer entre ouverture totale et pay-per-crawl (A40) avec un ordre de grandeur concret plutot qu'une decision a l'aveugle. Impact moyen / effort faible (donnee statique a rafraichir periodiquement, pas d'appel reseau au moment de l'audit). Implemente : nouveau message i18n statique `crawlRatioNote` (`report/i18n.ts`, EN/FR, chiffres Cloudflare Radar ClaudeBot ~20 600:1 et OpenAI ~1 300:1), affiche dans la section `ai-access` du rapport (HTML `report/html.ts` classe `.fam-info` + Markdown `report/markdown.ts`) — donnee statique fixe, aucun appel reseau, aucun impact sur le score. 5 tests ajoutes (html.test.ts, markdown.test.ts). Suite complete verte (1410 tests CLI + 238 web).

## Ajouts session automatique 2026-08-19 (phase recherche-ameliorations)

Signaux internes : suite complete verte 1410 tests CLI / 238 web, aucun TODO/FIXME. Veille externe (WebSearch 08/2026, evico.fr, seeklab.io, thegrowthgpt.com) : Chrome Lighthouse a ajoute une categorie d'audit **"Agentic Browsing"** qui verifie la presence d'un `llms.txt` (documentation Chrome officielle) ; l'adoption reste tres faible — 96,8 % des sites sans `llms.txt`, 41,3 % sans regle robots.txt specifique aux IA, seulement 24,4 % autorisant explicitement tous les crawlers IA majeurs ; les checklists d'audit IA 2026 citent aussi la presence d'un flux RSS/JSON a jour comme signal de fraicheur exploite par les crawlers IA — absent de nos checks actuels (verifie par grep, aucun check ne teste de flux). Sans doublon : note de transparence llms.txt = A34 (angle « inutile pour AI Overviews »), ratio crawl/referral = A54, roster Google-Agent = A53.

- [x] A55 [2026-08-19, livre] **Contexte informatif Lighthouse "Agentic Browsing" + stats d'adoption dans le check llms.txt** — meme pattern que A54 (donnee statique, pas de poids sur le score) : citer que Chrome Lighthouse audite desormais la presence de `llms.txt` sous ce nom et que 96,8 % des sites n'en ont pas encore, contextualise positivement un check aujourd'hui percu comme secondaire (cf A34) sans changer sa ponderation. Impact moyen (aide le client a prioriser) / effort faible. Implemente : texte `why` du check `llms-txt` complete (`report/check-i18n.ts`, EN+FR) avec la mention Lighthouse "Agentic Browsing" + le chiffre 96,8 % — pas de nouveau champ, pas de poids modifie, reutilise l'infrastructure de tooltip existante. Suite complete verte (1410 tests CLI).
- [x] A56 [2026-08-19, livre] **Nouveau check informatif : presence d'un flux RSS/JSON a jour** — les crawlers IA s'appuient dessus pour detecter la fraicheur du contenu (checklists d'audit 2026), signal absent de nos 120 checks actuels ; check leger (`<link rel="alternate" type="application/rss+xml">` ou `/feed`, `/rss.xml` standards, date du dernier item) dans la famille `llm-content` ou `technical-seo`. Impact moyen (comble un angle mort documente) / effort moyen (nouveau check + i18n + tests, pattern deja rode). Implemente : check `content-feed` (2 pts, famille `llm-content`, `checks/llm-content.ts`) — cherche un `<link rel="alternate" type="rss+xml|atom+xml|feed+json">` dans `<head>`, sinon essaie les chemins conventionnels (`/feed`, `/feed.xml`, `/rss.xml`, `/atom.xml`, `/index.xml`, `/feed.json`) same-origin uniquement, valide le format servi (XML rss/feed, ou JSON avec `items`). Avertissement seul (jamais d'echec), meme posture que `well-known-ai-json`. Scope volontairement limite a presence + format valide, sans logique de fraicheur/date (pas de flakiness liee au temps sur le serveur de fixtures). i18n (`check-i18n.ts` + `message-i18n.ts`) et documentation (`docs/guide.md`/`guide.fr.md`, `README.md`) mis a jour, fixture `perfect-site` complete (`feed.xml` + lien declare) pour preserver l'invariant score 100. Suite complete verte : 1415 tests CLI (89 fichiers).

## Ajouts session automatique 2026-08-19 (phase recherche-ameliorations, 3e passe)

Signaux internes : suite complete verte 1415 tests CLI / 238 web, aucun TODO/FIXME (grep sur `packages/cli/src` et `apps/web`). Veille externe (WebSearch 08/2026, digitalapplied.com, jetoctopus.com) : les guides 2026 sur la verification de crawlers IA insistent sur le fait que le user-agent seul ne prouve rien — les identifiants sont spoofables, et Cloudflare a documente des cas ou Perplexity a change de user-agent apres un blocage reseau (crawl furtif) ; la seule verification fiable cote serveur est le reverse-DNS (confirmer que l'IP source resout bien vers un domaine du fournisseur declare, ex. `*.googlebot.com`). Notre check `ai-crawlers-allowed` (roster 29 bots) ne teste que la presence de directives `Allow`/`Disallow` textuelles dans robots.txt — il ne peut techniquement pas faire de reverse-DNS (pas d'acces aux logs serveur du site audite), et ne le dit nulle part. Sans doublon : deadline Cloudflare 15/09/2026 = A30 (deja livre) ; roster Google-Agent = A53 (deja livre) ; ratio crawl/referral = A54 (deja livre).

- [x] A57 [2026-08-19, livre] **Note de transparence sur la limite structurelle du check `ai-crawlers-allowed` (declaratif, pas de reverse-DNS)** — meme pattern que A54/A55 (donnee informative statique, pas de poids sur le score) : preciser dans le texte `why` du check que le respect de robots.txt est un systeme honor-based (le crawler DOIT vouloir cooperer) et que ni cet outil ni aucun audit sans acces aux logs serveur ne peut confirmer qu'un bot se disant "GPTBot" ou "ClaudeBot" est authentique — seul un reverse-DNS cote serveur audite le confirmerait, hors de portee d'un audit externe. Evite de laisser croire au client qu'un score vert sur ce check garantit l'absence d'usurpation. Impact moyen (honnetete sur les limites de l'outil, credibilite renforcee plutot qu'affaiblie) / effort faible. Implemente : texte `why` du check `ai-crawlers-allowed` complete (`report/check-i18n.ts`, EN+FR), pas de nouveau champ ni de poids modifie. Au passage : corrige un test dogfooding rouge preexistant (`content-feed` warn sur `apps/web` lui-meme, introduit par A56) en publiant un vrai flux RSS a `/feed.xml` (reutilise `SITE_PAGES`/`lastmod`) declare via `<link rel="alternate">`. Suite complete verte : 1415 tests CLI + 238 web.

## Ajouts session automatique 2026-08-20 (phase recherche-ameliorations)

Signaux internes : monorepo sain (aucun TODO/FIXME nouveau), suites CLI/web deja vertes selon le journal (dernier run trace le 2026-08-19). Veille externe (WebSearch 08/2026, stackmatix.com, whitebunnie.com, sotaventomedios.com) : le risque emergent 2026 documente pour les donnees structurees est le **« schema drift »** (derive du schema) — le JSON-LD declare (prix, disponibilite, `dateModified`) diverge du contenu visible reellement affiche sur la page (prix change sans mise a jour du schema, statut de stock errone), ce qui erode fortement la confiance des moteurs IA dans la validation de la source ; les checklists d'audit AEO 2026 recommandent un controle trimestriel dedie a cette derive, distinct d'une simple validation de syntaxe JSON-LD (deja couverte par nos checks structured-data). Sans doublon : validation syntaxe/types JSON-LD = famille `structured-data` existante, presence llms.txt = A34/A55 (deja livres), roster bots OAI-SearchBot/Claude-SearchBot = deja dans `CITATION_BOTS` (verifie par grep, roster 29).

## Ajouts session automatique 2026-08-21 (phase recherche-ameliorations)

Signaux internes : suites completes vertes (1422 tests CLI + 243 web au dernier run trace, reconfirme cette session), aucun TODO/FIXME. Verifie par grep avant redaction : `cloudflare-ai-defaults` (A30, deja livre) couvre deja finement le changement de politique Cloudflare du 2026-09-15 (Training/Agent bloques par defaut, Search reste autorise) — piste ecartee car deja implementee a l'identique. Veille externe (SearXNG local, 6 requetes) : edikka.com (04/08/2026) confirme avec date precise que Google a retire les rich results FAQ de la recherche classique depuis le **7 mai 2026** (source Google Search Central), tout en rappelant qu'aucun balisage Schema.org special n'est requis pour les fonctionnalites generatives IA — la valeur du balisage FAQPage se deplace donc entierement vers le GEO, plus du tout vers l'affichage Google Search ; plusieurs guides RGAA/EAA 2026 (etcdigital.fr, nekoro.fr, stripe.com) confirment que le RGAA 5 (fin 2026) s'alignera sur les **9 nouveaux criteres WCAG 2.2**, dont le futur `sd-faq`/`accessibility.ts` du depot ne cite explicitement que WCAG 1.4.4 (zoom) — verifie par grep, aucune autre reference WCAG 2.2 dans `accessibility.ts`.

- [x] A62 [2026-08-21, livre] **Note de transparence sur la disparition des rich results FAQ Google (7 mai 2026) dans le check `sd-faq`** — le texte `why` actuel du check `sd-faq` (`report/check-i18n.ts`) presente le balisage FAQPage comme utile "so search and AI assistants can surface direct answers", ce qui laisse croire a un affichage Google Search toujours possible ; completer avec la date precise du retrait (7 mai 2026, source Google Search Central) evite de survendre un benefice Google qui n'existe plus, tout en confirmant que la valeur GEO (lisibilite par les assistants IA) reste intacte — meme pattern d'honnetete que A57 (limite reverse-DNS). Impact moyen (evite une promesse commerciale obsolete sur un check tres visible, credibilite renforcee) / effort quasi nul (texte statique EN+FR, pas de nouvelle collecte ni de poids modifie). Implemente : texte `why` du check `sd-faq` complete (`report/check-i18n.ts`, EN+FR) avec la date precise et la confirmation que la valeur GEO reste intacte ; retire aussi la reference obsolete a "search" pour ne garder que "AI assistants" dans la phrase de valeur. Pas de nouveau champ, pas de poids modifie. Suite complete verte : 1422 tests CLI.
- [x] A63 [2026-08-21, livre (partiel)] **Combler l'angle mort WCAG 2.2 avant l'alignement RGAA 5 (fin 2026)** — la famille `accessibility` ne cite explicitement que WCAG 1.4.4 (zoom desactive) ; WCAG 2.2 (publiee par le W3C en octobre 2023, sur laquelle le futur RGAA 5 s'alignera fin 2026 selon plusieurs guides 2026) ajoute 9 nouveaux criteres dont au moins deux sont statiquement verifiables sans navigateur headless : **2.5.8 Target Size Minimum** (zones cliquables/tactiles < 24x24px detectables via les dimensions CSS explicites quand elles sont declarees en dur) et **3.2.6 Consistent Help** (mecanisme d'aide/contact place dans un ordre relatif coherent d'une page a l'autre, verifiable via le crawl multi-pages deja en place, `sampledPages`). Impact moyen (anticipe une obligation reglementaire documentee et datee plutot que de la subir) / effort moyen (specifier precisement la detection statique avant implementation pour eviter les faux positifs, notamment sur target-size qui depend fortement du rendu reel — commencer par consistent-help, plus fiable en statique). Implemente : nouveau check `consistent-help` (`checks/accessibility.ts`, famille accessibility, 2 pts, evidence measured) — version deliberement restreinte de WCAG 2.2 3.2.6 : ne verifie PAS l'ordre relatif complet (trop de risque de faux positifs en statique, comme le redoutait la note d'effort ci-dessus), seulement que la presence d'un mecanisme d'aide (lien mailto:/tel: ou lien contact/support/aide/assistance) est coherente entre pages echantillonnees (skip si < 2 pages, skip si absent partout, warn seulement si present sur certaines pages et silencieusement absent sur d'autres). Enregistre dans `checks/index.ts`, textes `why`/`fix` EN+FR (`report/check-i18n.ts`), 4 gabarits de message traduits (`report/message-i18n.ts`), sections de doc ajoutees dans `guide.md`/`guide.fr.md`, compteurs mis a jour (144→145 checks, 107→108 mesures) dans README.md/packages/cli/README.md/les deux guides. **2.5.8 Target Size Minimum reste non fait** (risque de faux positifs trop eleve sans rendu reel, tel qu'anticipe) — a reconsiderer seulement avec une methode de detection plus sure. Suite complete verte : 1422 tests CLI + 243 web.

- A58 [2026-08-20] **Check « derive du schema » (schema drift) : comparer les valeurs JSON-LD declarees au contenu visible de la page** — pour les types deja detectes (Product/Offer : `price`, `availability` ; Article/WebPage : `dateModified`), verifier une coherence minimale avec le texte visible extrait du DOM (prix affiche vs prix du schema, presence d'une date de mise a jour recente vs `dateModified` declare) ; alerter (warn, pas fail, pour eviter les faux positifs de formatage) en cas de divergence detectable. Signal source par plusieurs guides AEO 2026 comme cause principale de perte de confiance des moteurs IA sur des donnees structurees par ailleurs syntaxiquement valides. Impact fort (personne d'autre dans le comparatif A39 ne le fait, differenciant concret) / effort moyen (extraction + comparaison texte/JSON-LD, risque de faux positifs a cadrer soigneusement avant livraison — commencer par le cas Product/Offer le plus simple).
- [x] A59 [2026-08-20, livre] **Rappel de frequence d'audit recommandee dans le rapport (trimestriel)** — les guides AEO 2026 recommandent un cycle d'audit trimestriel pour eviter la derive de schema (A58) et le contenu perime ; ajouter une note contextuelle en pied de rapport HTML/Markdown suggerant une re-verification a echeance 90 jours (complement leger a A35 « mode audit recurrent/tendance », qui reste l'implementation complete de suivi dans le temps). Impact faible-moyen (pedagogie client, argument de vente pour un suivi recurrent) / effort quasi nul (texte statique, pas de nouvelle donnee). Implemente : nouveau champ `auditFrequencyNote` (`report/i18n.ts`, EN+FR, meme pattern que `crawlRatioNote`) affiche une fois en pied de rapport HTML (`<footer>`, classe `fam-info` reutilisee) et Markdown (ligne italique avant `mdFooter`). Pas de nouveau poids, pas de nouvelle donnee. Suite complete verte : 1415 tests CLI + 238 web.

## Ajouts session automatique 2026-08-20 (phase recherche-ameliorations, 2e passe)

Signaux internes : suite complete verte (1415 tests CLI + 238 web, dernier run trace), aucun TODO/FIXME. Le pattern de dogfooding (`apps/web/test/dogfooding.test.mjs`, a deja revele 3 bugs produits reels le 26/07 puis un test rouge preexistant corrige par A57 le 19/08) ne s'applique qu'a `apps/web` (findable.bordebat.fr) alors que le meme moteur est deploye sur 2 autres sites de l'ecosysteme (pb-ot.fr, vigie.bordebat.fr) sans jamais etre rejoue dessus. Veille externe (WebSearch 08/2026, github.com/vedangvatsa/aistandards) : plusieurs vendeurs 2026 documentent une pratique fine de robots.txt qui separe explicitement les bots d'entrainement (GPTBot) des bots de recherche/citation (OAI-SearchBot, Claude-SearchBot) — bloquer les premiers sans bloquer les seconds est une configuration deliberee et sophistiquee, pas une erreur. Verifie par grep : `packages/cli/src/robots.ts` distingue deja `TRAINING_BOTS`/`CITATION_BOTS`, et `ai-crawlers-allowed` ne penalise que le blocage des bots de citation (correct) — mais rien ne signale positivement au client quand il a deja cette configuration fine. Sans doublon : roster 29 bots = LOT 3 (deja livre), ratio crawl/referral = A54 (deja livre), Google-Agent = A53 (deja livre).

- [x] A60 [2026-08-20, livre] **Etendre le pattern de dogfooding CI a pb-ot.fr et vigie.bordebat.fr (canaris multi-sites)** — `dogfooding.test.mjs` fait deja tourner le vrai moteur d'audit contre `apps/web` en CI et a deja debusque 3 bugs produits reels (regression `www-consolidation`, regex intertitre EN-only, `isSelfSufficientStart` trop strict) plus un test rouge corrige par A57 ; etendre le meme mecanisme (fixtures ou fetch live controle) aux 2 autres sites vivants de l'ecosysteme deployes avec le meme moteur (pb-ot.fr, vigie.bordebat.fr) transformerait chaque evolution de check en garde-fou de non-regression sur 3 sites reels au lieu d'un seul, sans travail de decouverte de bugs supplementaire (le mecanisme existe deja). Impact moyen-fort (multiplie par 3 la surface de detection de faux positifs/negatifs avant deploiement) / effort faible (reutilise l'infra dogfooding existante, pas de nouveau code de check). Implemente : nouveau fichier `apps/web/test/dogfooding-ecosystem.test.mjs`, fetch live de pb-ot.fr (skip propre, pas d'echec, si injoignable — verifie manuellement : 200 OK), audit reel (`maxPages: 4`) via le meme moteur. Contrairement a `dogfooding.test.mjs` (site que l'on controle), aucune assertion sur un ensemble exact de checks non passants n'est faite : pb-ot.fr evolue independamment de ce depot, un tel pin serait fragile pour de mauvaises raisons. Assertions robustes aux deux repos : le crawl aboutit, chaque check resout un statut connu (jamais de crash silencieux), le score reste fini et dans [0,100], et 3 checks sans raison legitime de regresser (`www-consolidation`, `agent-usability`, `ai-crawlers-allowed`) restent verts. vigie.bordebat.fr volontairement exclu : verifie manuellement que tout le site repond 401 (auth HTTP sur toutes les routes, y compris `/login`) — impossible de le crawler sans embarquer un identifiant dans les tests (regle « aucun secret touche »), et c'est un tableau de bord prive, pas un site public dont la « findabilite » par des crawlers IA a un sens a auditer. Suite complete verte : 1415 tests CLI + 243 web (238 + 5 nouveaux).
## Ajouts session automatique 2026-08-21 (phase recherche-ameliorations, 2e passe)

Signaux internes : suite complete verte (1422 tests CLI + 243 web, reconfirme cette session), aucun TODO/FIXME (grep sur `packages/cli/src` et `apps/web`). Comptage direct par grep sur `checks/structured-data.ts`/`structured-data-mp.ts` : ~29 types Schema.org distincts reellement couverts (Article, BlogPosting, NewsArticle, TechArticle, FAQPage, QAPage, HowTo, Recipe, Event, Product, LocalBusiness + sous-types (Bakery, Cafe, Restaurant, Store...), Organization + sous-types, Person, WebSite/SearchAction, BreadcrumbList/ListItem, VideoObject). Veille externe (SearXNG local + WebFetch, 08/2026) : score-geo.fr publie un comparateur chiffre face a Semrush/Ahrefs/Sistrix/Ubersuggest ou le nombre de types Schema.org couverts est mis en avant comme critere de decision explicite (Score GEO revendique 12 types vs 4-6 chez les autres) ; Google Search Console a deploye mi-2026 un nouveau rapport "Performance IA generative" dedie (impressions AI Overviews/AI Mode/Discover, dimensions pages/pays/appareils/dates) mais **sans donnee de clics** et avec un bug de logging confirme par Google le 13/08/2026 (baisse d'impressions non representative d'une vraie perte de visibilite). Sans doublon : positionnement general face aux suites GEO = A46 (deja livre, angle "audit vs monitoring", pas de comparaison chiffree de couverture Schema.org) ; correlation citation/trafic GA4-Search Console = V49 (cote vigie-seo, mesure recurrente, pas ce rapport specifique nouveau).

- [x] A64 [2026-08-21, livre] **Positionnement chiffre "couverture Schema.org" face aux comparatifs 2026** — completer la section de positionnement du README avec le nombre reel de types Schema.org couverts par la famille `structured-data` (~29 types distincts, reverifie par grep sur `checks/structured-data.ts` avant redaction), a mettre en regard du chiffre revendique par le comparateur score-geo.fr (12 types chez le mieux note, 4-6 chez Semrush/Ahrefs/Sistrix/Ubersuggest). Implemente : nouveau paragraphe "Structured-data coverage, in numbers." dans `README.md` (section "How it compares"), liste des familles de types couverts. Pas de section equivalente dans `packages/cli/README.md` (verifie, la section "How it compares" n'existe que dans le README racine). Suite complete verte : 1422 tests CLI + 243 web (aucun test dedie necessaire, texte statique comme A46/A57).
- [x] A65 [2026-08-21, livre] **Note de transparence sur le nouveau rapport "Performance IA generative" de Google Search Console** — note pedagogique (meme pattern que `auditFrequencyNote`/A59, informative, sans poids sur le score) recommandant au client de consulter ce rapport gratuit recemment deploye par Google (impressions dans AI Overviews/AI Mode/Discover) comme complement de mesure gratuit a l'audit ponctuel findable-audit, avec la limite connue explicitee (pas de donnee de clics, bug de logging confirme par Google le 13/08/2026). Implemente : nouveau champ i18n `gscAiPerformanceNote` (`report/i18n.ts`, EN+FR, meme pattern que `auditFrequencyNote`), affiche une fois en pied de rapport HTML (`report/html.ts`, juste apres `auditFrequencyNote`) et Markdown (`report/markdown.ts`, meme emplacement). Pas de nouveau poids, pas de nouvelle collecte. Suite complete verte : 1422 tests CLI + 243 web.

## Ajouts session automatique 2026-08-22 (phase recherche-ameliorations)

Signaux internes : suite complete verte (1422 tests CLI + 243 web, reconfirme cette session, `npx vitest run` + `npm run test:web`), aucun TODO/FIXME (grep sur `packages/cli/src` et `apps/web`). Verifie par grep avant redaction : `FAMILY_WEIGHTS['llm-content'] = 0.18` (`packages/cli/src/scoring.ts`) et la famille compte 21 checks ; aucune mention de "Rich Results Test" ni "Schema.org Validator" dans README.md/packages/cli/README.md/docs/apps/web. Veille externe (SearXNG local, 08/2026) : la grille d'audit publique Yvarn (47 criteres, licence CC-BY-NC 4.0, studio Bordeaux, mise a jour 2026-Q2) traite le sujet "IA et visibilite LLM" comme une 7e categorie ajoutee en 2026, pesant seulement 4 criteres sur 47 (5-10% du score selon le profil de site) — citations LLM, contenu AI-readable, llms.txt, Entity SEO/Wikidata. C'est exactement le domaine que findable-audit couvre en profondeur (famille `llm-content`, 18% du score pondere, 21 checks distincts). Sans doublon : positionnement general vs suites GEO = A46 (deja livre, angle "audit vs monitoring") ; couverture Schema.org chiffree vs score-geo.fr = A64 (deja livre, comparateur different, critere different).

- [x] A66 [2026-08-22, livre] **Positionnement chiffre face a la grille d'audit publique Yvarn (47 criteres, 2026)** — completer la section "How it compares" du README avec un paragraphe comparant le traitement du sujet GEO/IA : Yvarn le reduit a 4 criteres sur 47 (une 7e categorie ajoutee en 2026, ponderee 5-10%), quand findable-audit y consacre une famille entiere de 21 checks ponderee a 18% du score. Angle complementaire a A64 (qui compare la couverture Schema.org face a score-geo.fr) : ici la comparaison porte sur la profondeur de traitement du sujet IA/GEO lui-meme face a un audit SEO classique qui l'ajoute en marge. Impact moyen (argument de positionnement chiffre et verifiable, source publique citable) / effort quasi nul (texte statique dans README.md, meme pattern que A64 — pas de nouveau check, pas de nouveau champ i18n). Implemente : nouveau paragraphe "How deep, compared to a general SEO audit." dans README.md, juste avant "Where alternatives beat us today". Pas de test dedie necessaire (texte statique, meme pattern que A64).
- [x] A67 [2026-08-22, livre] **Lien vers les outils de verification externes gratuits (Google Rich Results Test, Schema.org Validator) dans le guide** — le guide (`guide.md`/`guide.fr.md`) explique les checks `structured-data`/`sd-*` mais ne mentionne aucun des deux outils de reference gratuits que Google et Schema.org publient pour que le client verifie lui-meme son balisage JSON-LD apres correction ; la grille Yvarn les cite explicitement comme etape de validation (categorie B, critere 18). Impact faible-moyen (aide le client a auto-verifier une correction sans re-audit complet, credibilite) / effort quasi nul (2 liens + 1 paragraphe dans le guide EN+FR, aucun changement de code). Implemente : paragraphe "Verifying a fix yourself"/"Verifier une correction soi-meme" en tete de la section "Structured data & metadata"/"Donnees structurees et metadonnees" (`docs/guide.md`, `docs/guide.fr.md`), liens vers search.google.com/test/rich-results et validator.schema.org.

- [x] A61 [2026-08-20, livre] **Note informative : configuration fine training-bots-bloques / citation-bots-autorises deja detectee mais jamais valorisee** — le roster `TRAINING_BOTS`/`CITATION_BOTS` (`robots.ts`) permet deja de distinguer les deux categories, et le check `ai-crawlers-allowed` verifie deja correctement que seuls les bots de citation comptent pour le score ; ajouter une note purement informative (meme pattern que `crawlRatioNote`/A54, aucun poids) quand le robots.txt du site audite bloque explicitement au moins un bot d'entrainement (GPTBot) tout en autorisant les bots de citation — signale au client qu'il applique deja une pratique 2026 avancee documentee par plusieurs vendeurs (aistandards.md), plutot que de laisser cette configuration invisible dans le rapport. Impact faible-moyen (valorise une bonne pratique deja presente chez certains clients, credibilite) / effort faible (donnee deja disponible via `TRAINING_BOTS`/`CITATION_BOTS`, pas de nouvelle collecte). Implemente : nouveau champ i18n `trainingBotsBlockedNote` (`report/i18n.ts`, EN+FR, meme pattern que `crawlRatioNote`/A54), affiche dans la famille `ai-access` (HTML `report/html.ts` + Markdown `report/markdown.ts`) uniquement quand le resultat du check `ai-crawlers-allowed` est `warn` avec `messageTemplate` commencant par `AI crawlers blocked:` — condition qui, par construction du check (`checks/ai-access.ts`), signifie exactement « au moins un bot bloque, aucun bot de citation parmi eux » (sinon le statut serait `fail`), sans reimplementer la logique de detection ni faire de nouvelle collecte. Tests dedies ajoutes (html.test.ts + markdown.test.ts, EN+FR, cas warn/fail/pass). Suite complete verte : 1422 tests CLI + 243 web.

## Ajouts session automatique 2026-08-22 (phase recherche-ameliorations, 2e passe)

Signaux internes : suite complete verte (1422 tests CLI + 243 web, reconfirme cette session), aucun TODO/FIXME. Veille externe (WebFetch, writesonic.com "Best GEO Tools 2026", comparatif 6 criteres d'outils de monitoring GEO) : l'article documente que la couche de service par user-agent de Scrunch AI (AXP) sert un contenu different selon le visiteur, et la qualifie explicitement de "cloaking-shaped risk" — soit exactement le schema que le check `ai-serving-parity` (#20) sonde deja depuis son cote crawl (comparaison GPTBot/ClaudeBot/navigateur mobile). Sans doublon : positionnement general des checks GEO = A46/A64/A66 (deja livres, angles differents — audit vs monitoring, couverture Schema.org, profondeur Yvarn) ; aucun ne cite d'exemple reel documente de la technique que ce check detecte.

- [x] A68 [2026-08-22, livre] **Preuve reelle documentee pour le check `ai-serving-parity` (cloaking/dynamic-serving)** — le check compare deja le HTML servi a GPTBot/ClaudeBot/mobile contre celui servi par defaut, mais rien dans le guide ne montre qu'il s'agit d'un risque reellement deploye plutot que theorique. Un comparatif 2026 d'outils GEO (Writesonic, "Best GEO Tools 2026") signale que la couche AXP d'un concurrent (Scrunch AI) sert un contenu different par user-agent et la qualifie lui-meme de "cloaking-shaped risk" — exactement le motif que `ai-serving-parity` detecte. Impact faible-moyen (credibilite : le check ne detecte pas un risque hypothetique, un exemple documente existe deja dans l'ecosysteme GEO 2026) / effort quasi nul (1 phrase sourcee ajoutee au paragraphe "Why" du check, EN+FR, aucun changement de code ni de logique de detection). Implemente : phrase ajoutee dans `docs/guide.md`/`docs/guide.fr.md`, section `ai-serving-parity`. Suite `test/docs` verte (15 tests), aucun changement de code de check.

## Ajouts session automatique 2026-08-23 (phase recherche-ameliorations)

Signaux internes : suites completes vertes (1422 tests CLI + 243 web, reconfirme cette session avec la bonne invocation par module — `npx vitest run` pour packages/cli, `npm test` dans apps/web pour le runner node:test separe), aucun TODO/FIXME (grep sur `packages/cli/src` et `apps/web`). Veille externe (SearXNG local, WebSearch 08/2026) : une analyse SparkToro du clickstream Similarweb (jan-avr 2026) chiffre a **~68 % la part des recherches Google qui se terminent sans aucun clic**, en hausse continue ; separement, l'echelle des moteurs IA concurrents de la recherche classique est desormais documentee precisement — ChatGPT ~900 M d'utilisateurs actifs hebdomadaires (OpenAI, fevrier 2026, rapporte par TechCrunch), Gemini app > 750 M d'utilisateurs actifs mensuels (resultats Google T1 2026), AI Overviews (Gemini) touchant plus de 2 milliards de personnes par mois. Verifie par grep : aucune de ces statistiques n'apparait dans `README.md`/`packages/cli/README.md`. Separement, plusieurs checklists GEO/E-E-A-T 2026 (ailabsaudit.com, triomize.com, unfoldmart.com, mo.agency) recommandent la declaration d'un auteur en schema Person + liens `sameAs`, unfoldmart.com qualifiant explicitement LinkedIn de "signal le plus fort pour l'autorite d'auteur en 2026" ; verifie par grep sur `checks/llm-content.ts` et `checks/sameas-verified.ts` : le check `content-author-eeat` couvre deja la declaration Person/sameAs, et `sameas-verified` va plus loin que toutes ces checklists generiques en verifiant que les liens `sameAs` sont **reciproques** (le profil externe pointe bien en retour vers le site), pas seulement declares. Sans doublon : positionnement general vs suites GEO = A46/A64/A66 (deja livres, angles differents — audit vs monitoring, couverture Schema.org, profondeur Yvarn) ; aucun des trois ne cite ni la statistique zero-clic/echelle IA, ni la comparaison E-E-A-T/sameAs face aux checklists generiques 2026.

- [x] A69 [2026-08-23, livre] **Ajouter la statistique zero-clic (68 %, SparkToro 2026) et l'echelle des moteurs IA (ChatGPT ~900M WAU, Gemini 750M+ MAU, AI Overviews 2Md+/mois) au positionnement README** — completer la section d'ouverture ou "How it compares" du README avec ces chiffres sources et dates pour etayer, hors de toute affirmation marketing non sourcee, pourquoi l'audit de findabilite IA (et non plus seulement le SEO classique) devient un sujet a part entiere. Impact moyen (argument d'ouverture chiffre et sourcable, meme registre que A64/A66) / effort quasi nul (texte statique dans README.md, meme pattern que A64/A66 — pas de nouveau check, pas de nouveau champ i18n, pas de test dedie necessaire).
  Implemente : nouveau paragraphe "Why this matters, in numbers." juste avant "How it compares (honestly)" dans README.md. 1422 tests CLI + 243 web verts.
- [x] A70 [2026-08-23, livre] **Positionnement : les checks `content-author-eeat`/`sameas-verified` depassent deja les checklists E-E-A-T/sameAs generiques 2026** — completer la section "How it compares" du README avec un paragraphe expliquant que plusieurs checklists GEO 2026 (dont unfoldmart.com, qui cite LinkedIn comme "signal le plus fort pour l'autorite d'auteur en 2026") se limitent a demander la **presence declarative** d'un schema Person + liens `sameAs`, alors que `sameas-verified` verifie deja la **reciprocite reelle** du lien (le profil externe pointe en retour vers le site) — differenciateur concret verifie par grep avant redaction, pas une reformulation marketing. Impact moyen (argument de positionnement chiffre et techniquement precis, credibilite renforcee sur un sujet ou beaucoup d'outils se contentent du declaratif) / effort quasi nul (texte statique, meme pattern que A64/A66/A69 — aucun changement de check).
  Implemente : nouveau paragraphe "Verified, not just declared, E-E-A-T signals." dans README.md, juste avant "How deep, compared to a general SEO audit." 1422 tests CLI + 243 web verts.

## Ajouts session automatique 2026-08-23 (phase recherche-ameliorations, 2e passe)

Signaux internes : suites completes vertes (1422 tests CLI + 243 web, reconfirme cette session), aucun TODO/FIXME (grep sur `packages/cli/src` et `apps/web`). Aucun point de friction nouveau dans `report/` (html.ts/markdown.ts/recommendations.ts), le backlog a deja traite l'essentiel des irritants d'ergonomie. Veille externe (WebSearch 08/2026) : RSL (Really Simple Licensing) 1.0 finalise decembre 2025, soutenu par ~1500 editeurs (Reddit, Yahoo, Medium, Quora, AP) et par Cloudflare/Akamai/Fastly cote enforcement — la table finale du backlog l'ecartait avec TDMRep comme "pile pas stabilisee", ce qui n'est plus vrai specifiquement pour RSL (TDMRep, lui, reste a ~45 hosts, adoption quasi nulle). Un audit 2026 de 30 llms.txt en production (derivatex.agency, seoscore.tools) documente que l'echec le plus frequent est de traiter le fichier comme un second sitemap non trie (800-1200 liens) plutot qu'un index cure (20-50 liens a forte valeur) — angle mort confirme par grep sur `checks/llms-lint.ts` (aucune verification de densite/comptage). Confirme par grep : aucun des ~29 types Schema.org couverts n'inclut `SpeakableSpecification`, dont plusieurs guides 2026 (agentceres.com, aiso-hub.com) documentent la reinterpretation comme signal d'extraction pour Perplexity/ChatGPT-browsing au-dela de l'usage historique Google Assistant. Sans doublon : transparence llms.txt = A34 (angle utilite AI Overviews, pas densite), ratio crawl/referral = A54, roster Google-Agent = A53, couverture Schema.org chiffree vs concurrents = A64.

- [x] A71 [2026-08-23, livre] **Check RSL (Really Simple Licensing) — standard de licensing IA desormais stabilise** — nouveau check dans `checks/ai-access.ts` (meme pattern que `pay-per-crawl`/A40) detectant la presence de balises RSL (`<link rel="license" href=".../rsl.xml">`, directive `License:` dans robots.txt, ou fichier RSL dedie) ; avertissement informatif (pas de poids fort), signale si le site declare des conditions de licensing IA (crawling/training/inference). A distinguer de TDMRep, toujours a ecarter (adoption quasi nulle). Impact moyen / effort faible (reutilise le pattern `pay-per-crawl` deja en place). Implemente : check `rsl-license` (2 pts, evidence measured) dans `checks/ai-access.ts`, enregistre dans `buildChecks()` ; entrees `CHECK_TITLES`/`CHECK_I18N` (check-i18n.ts) et traductions FR (message-i18n.ts) ; sections EN/FR dans `docs/guide.md`/`docs/guide.fr.md` ; 5 tests unitaires dans `ai-access.test.ts` ; fixture `perfect-site` mise a jour (`<link rel="license">` dans le head) pour garder l'invariant 100/A. Catalogue de checks 145 -> 146 (109 mesures + 37 heuristiques), README/guides remis a jour. 1427 tests CLI + 243 web verts.
- A72 [2026-08-23] **Densite/curation du llms.txt, distinct du lint de format existant** — etendre `checks/llms-lint.ts` (ou check compagnon) pour avertir quand le fichier ressemble a un second sitemap non trie (des centaines de liens) plutot qu'a un index cure (recommandation 20-50 liens a forte valeur signal). Le check `llms-txt-lint` actuel (A1) verifie deja format/liens/resolution HTTP mais jamais la densite. Impact moyen / effort faible-moyen (reutilise l'infrastructure existante, ajoute un seuil de comptage).
- [x] A73 [2026-08-24, livre] **Check `SpeakableSpecification` (schema.org) manquant dans la famille structured-data** — nouveau check `sd-speakable` dans `checks/structured-data.ts`, detectant la presence de `SpeakableSpecification` (`cssSelector`/`xpath`) sur les pages Article/NewsArticle/BlogPosting/TechArticle/FAQPage/HowTo, en advisory (warn si absent ou sans selecteur, skip si aucun type eligible). Renforce l'argument chiffre de couverture Schema.org deja revendique (A64/A66) en comblant un trou reel plutot que de le laisser vulnerable a un futur audit contradictoire. Impact faible-moyen / effort faible (pattern deja rode sur les autres checks structured-data).
  Implemente : check `sd-speakable` (1 pt, evidence measured) dans `checks/structured-data.ts`, enregistre dans `buildChecks()` ; entrees `CHECK_TITLES`/`CHECK_I18N` (check-i18n.ts) et 4 traductions FR (message-i18n.ts) ; sections EN/FR dans `docs/guide.md`/`docs/guide.fr.md` ; 4 tests unitaires (skip/warn/warn/pass) dans `structured-data.test.ts`. Fixture `perfect-site` ne declare aucun type eligible (Article/FAQPage/HowTo) donc le check y skip, invariant 100/A preserve sans modification. Catalogue de checks 146 -> 147 (110 mesures + 37 heuristiques), README/guides/`runner.test.ts` remis a jour. 1431 tests CLI verts.
- [x] A74 [2026-08-23, livre] **Bug trouve pendant l'implementation d'A71 : le check `pay-per-crawl` (A40, livre le 2026-08-09, teste unitairement dans `ai-access.test.ts`) n'etait jamais enregistre dans `buildChecks()` (`checks/index.ts`)** — importe mais absent du tableau retourne, donc jamais execute lors d'un audit reel malgre sa documentation dans `docs/guide.md`/`docs/guide.fr.md` et ses cles i18n dans `report/check-i18n.ts`/`report/message-i18n.ts`. Impact moyen (une fonctionnalite documentee et testee etait silencieusement absente de tout audit reel depuis sa livraison) / effort quasi nul (1 ligne, insertion dans le tableau a cote de `cloudflareAiDefaults`). Implemente : `payPerCrawl` ajoute a la liste retournee par `buildChecks()` ; `packages/cli/test/runner.test.ts` mis a jour (143 -> 144 checks executes sur la fixture `llm-good`, aucun changement au jeu de checks skippes). 1422 tests CLI + 243 web verts.

## Ajouts session automatique 2026-08-24 (phase recherche-ameliorations)

Signaux internes : suites completes vertes selon le dernier run trace au journal (1427 tests CLI + 243 web), aucun TODO/FIXME (grep sur `packages/cli/src` et `apps/web`). Verifie par grep : `performance-cwv.ts` utilise deja les 3 metriques CWV a jour (LCP/CLS/INP, pas l'ancien FID) — piste ecartee, deja conforme. Verifie par grep sur README.md/docs/guide.md : "Lighthouse" est deja cite en comparatif (ligne 306, "no GEO layer") mais sans mention de sa nouvelle categorie 2026. Veille externe (SearXNG local, 08/2026) : le texte `why` du check `llms-txt` lui-meme mentionne deja que Chrome Lighthouse audite depuis 2026 la presence de llms.txt sous sa propre categorie "Agentic Browsing" (echantillon 2026 : 96,8% des sites toujours depourvus) — cette information existe en base de code mais n'est jamais remontee dans le tableau de positionnement du README, qui reste generique ("no GEO layer"). Sans doublon : couverture Schema.org chiffree = A64 (deja livre, comparateur different) ; profondeur GEO vs grille Yvarn = A66 (deja livre, comparateur different) ; aucun des deux ne cite Lighthouse/Agentic Browsing specifiquement.

- [x] A75 [2026-08-24, livre] **Positionnement chiffre face a la nouvelle categorie Lighthouse "Agentic Browsing" (2026)** — completer la ligne de comparaison Lighthouse du tableau "How it compares" (README.md, ligne ~306) avec le fait, deja source et documente dans le propre texte `why` du check `llms-txt` de ce depot, que Lighthouse se limite a verifier la presence de llms.txt (1 fichier, 1 signal binaire) alors que findable-audit y consacre une famille entiere de checks (`llm-content`, 18% du score). Angle complementaire a A66 (qui compare a une grille SEO generaliste tierce) : ici la comparaison porte sur l'outil de reference historique de l'audit technique lui-meme, qui rattrape a peine le sujet IA en 2026. Impact moyen (comparateur credible car c'est l'outil que tout auditeur technique connait deja) / effort quasi nul (1 phrase sourcee, donnee deja presente ailleurs dans le code, aucun nouveau check).
  Implemente : nouveau paragraphe "How we compare to the audit tool every technical SEO already runs." dans README.md, juste apres le paragraphe Yvarn/`llm-content`. Bonus : le chiffre `llm-content` cite juste au-dessus etait perime (21 checks au moment d'A75, verifie par grep sur `packages/cli/src/checks/*.ts` : 23 checks reels avec `family: 'llm-content'` — 15 dans `llm-content.ts`, `agent-usability` dans `agentic.ts`, 4 dans `geo-advanced.ts`, 2 dans `geo-retrieval.ts`, `llms-txt-lint` dans `llms-lint.ts`) ; corrige au passage (21 -> 23) puisque le paragraphe voisin cite desormais le meme chiffre.

## Ajouts session automatique 2026-08-24 (phase recherche-ameliorations, suite)

- [x] A76 [2026-08-24, clos (decision documentee, pas d'implementation)] **Verifier/clore la piste WCAG 2.2 Target Size Minimum (2.5.8), laissee explicitement de cote par A63 faute de methode statique fiable** — A63 (2026-08-21) a implemente `consistent-help` mais a explicitement renonce a `target-size` par crainte de faux positifs sans rendu reel du navigateur. Veille de cette session (SearXNG local) n'a trouve aucune methode de detection statique fiable nouvelle (outils communautaires cites — axe-core, WAVE — s'appuient tous sur un rendu DOM reel/headless, absent de l'architecture actuelle du crawler). Item de type "verification/decision documentee" plutot que nouvelle piste : soit clore explicitement 2.5.8 au backlog avec la raison (necessite Puppeteer/Playwright, hors du perimetre fetch-only actuel), soit le reformuler comme prerequis a une evolution architecturale plus large (deja evoquee mais jamais tranchee dans le journal). Impact faible (clarifie une decision en suspens plutot que d'ajouter une fonctionnalite) / effort quasi nul (mise a jour de note au backlog).
  **Decision (2026-08-24) : clos, pas de reformulation en prerequis.** L'architecture actuelle du crawler est fetch-only (`undici`, pas de rendu DOM/headless — verifie par grep : aucune dependance `puppeteer`/`playwright` dans `packages/cli/package.json`), et le reste du backlog GEO (A72-A75 en cours cette session) ne signale aucun besoin fonctionnel qui justifierait a lui seul d'introduire un navigateur headless — le seul autre candidat historique a un rendu reel etait le CWV lab (deja couvert sans headless local via l'API PSI, cf `performance-cwv.ts`). Sans second driver, transformer 2.5.8 en "prerequis architectural" creerait une dette de decision fantome (une piste qui attend indefiniment un declencheur qui n'existe pas). WCAG 2.5.8 Target Size Minimum reste donc **hors perimetre**, a rouvrir uniquement si un besoin independant (ex. rendu reel pour CSR/cloaking) justifie un jour l'ajout d'un navigateur headless au crawler — pas avant. Aucun changement de code, pas de nouveau check, decision actee ici pour eviter qu'un futur passage recherche-ameliorations ne la re-decouvre a l'identique.

## Ajouts session automatique 2026-08-24 (phase recherche-ameliorations, 2e passe)

Signaux internes : aucun TODO/FIXME (grep `packages/cli/src`/`apps/web`), suite verte au dernier run trace (1431 tests CLI + 243 web). Veille externe (SearXNG local, 08/2026) : le groupe de travail IETF `aipref` (AI Preferences) fait progresser deux drafts standard-track — `draft-ietf-aipref-vocab` (vocabulaire de preferences IA) et `draft-ietf-aipref-attach` (association HTTP/robots.txt de ces preferences a un contenu) — positionnes explicitement comme la suite officielle et normalisee des initiatives ad hoc actuelles (`ai.txt`, TDMRep, RSL) que le catalogue "Matrice politiques IA" du backlog decrit deja comme "pile pas stabilisee". C'est une confirmation datee (drafts actifs IETF, pas encore RFC) de pourquoi cette pile reste instable, et un signal a surveiller plutot qu'un nouveau check a coder maintenant (norme non finalisee, adoption navigateur/crawler nulle a ce stade). Sans doublon : `rsl-license` (A71, deja livre, standard stabilise et deployable) est un cas distinct et deja mesure ; `tdmrep`/`ai.txt` sont deja cites comme "adoption quasi nulle" dans A71 mais sans reference a leur remplacement normatif en cours.

- [x] A77 [2026-08-24, livre] **Note de veille : le futur standard IETF AIPREF (draft-ietf-aipref-attach/vocab) va potentiellement unifier ai.txt/TDMRep/RSL** — documenter (texte `why` informatif, meme registre que A54/A55/A57, pas de nouveau check ni de poids) que l'IETF travaille sur un vocabulaire et un mecanisme d'attachement HTTP normalises pour les preferences IA, destines a terme a remplacer la mosaique actuelle de fichiers ad hoc (`ai.txt`, TDMRep, RSL) evoquee dans la "Matrice politiques IA" du backlog. Utile pour repondre par avance a un client qui demanderait "pourquoi ne pas juste tout mettre dans ai.txt ?" avec une reponse sourcee (norme encore en draft, pas de RFC, adoption nulle) plutot qu'une explication vague. A re-verifier avant implementation de tout nouveau check : si le WG publie un RFC avant la prochaine passe, la matrice politiques IA pourrait alors devenir un vrai check plutot qu'une simple note. Impact faible (credibilite/anticipation, pas de fonctionnalite mesurable immediate) / effort quasi nul (1 note sourcee, pas de nouveau check).
  Implemente : phrase de veille sourcee (draft-ietf-aipref-vocab/attach) ajoutee au texte `why` EN+FR du check `rsl-license` (`packages/cli/src/report/check-i18n.ts`), meme registre que le reste du texte (standard emergent, adoption a suivre). Aucun nouveau check, aucun changement de score. 1431 tests CLI verts.

## Ajouts session automatique 2026-08-25 (phase recherche-ameliorations)

Signaux internes : aucun TODO/FIXME (grep `packages/cli/src`), suite verte au dernier run trace (1431 tests CLI). Pistes ecartees avant redaction car deja couvertes : plan d'action priorise "quick wins" (A48, `report/axes.ts`, priority bars dans `report/html.ts`), check auteur/E-E-A-T (`content-author-eeat` dans `llm-content.ts`), blocage Cloudflare par defaut (`cloudflare-ai-defaults` dans `ai-access.ts`), sondes UA differentielles GPTBot/ClaudeBot (deja dans `ai-access.ts`). Veille externe (SearXNG local, 08/2026) : adoption reelle de llms.txt mesuree a 10,13 % sur 300k domaines (SE Ranking, janv. 2026, relaye par dynamic-creative.com 03/2026) et consensus presse FR 2026 "aucun grand moteur ne confirme l'usage" (junto.fr 06/2026, ellevate.fr 04/2026) ; gestion des crawlers IA 2026 centree robots.txt + WAF/anti-bot (alicelabs.ai, attrifast.com) — la partie anti-bots tiers (hors Cloudflare) n'est couverte par aucun check.

- [x] A78 [2026-08-25] (livre 2026-08-26) **Sourcer le taux d'adoption reel de llms.txt dans le texte `why` du check llms.txt** — completer le `why` EN+FR (registre honnetete, meme veine que A77) avec la donnee chiffree : adoption 10,13 % sur 300k domaines (SE Ranking janv. 2026) et absence de confirmation officielle de consommation par OpenAI/Anthropic/Perplexity (silence documente, presse FR 2026). Permet de repondre de facon sourcee a "pourquoi maintenir un llms.txt si personne ne confirme le lire ?" : standard emergent, cout quasi nul, pari asymetrique — sans survendre. Verifier a l'implementation que le `why` actuel (`report/check-i18n.ts`, entree llms.txt) ne contient pas deja de chiffre d'adoption (grep 2026-08-25 : aucun). Impact faible-moyen (credibilite du rapport, anticipation d'objection client) / effort quasi nul (2 phrases sourcees, 0 nouveau check).
- [ ] A79 [2026-08-25] **Diagnostic dedie des anti-bots tiers (CleanTalk, DataDome, Imperva...) qui bloquent les clients HTTP legitimes** — cas reel rencontre en prospection PB OpenTech le 2026-08-24 : site vitrine TPE derriere CleanTalk renvoyant 403 a tout client HTTP standard mais 200 a l'UA GPTBot (cloaking inverse). Aujourd'hui un tel site fait echouer le fetch baseline avec une erreur generique au lieu d'un diagnostic actionnable. Idee : reconnaitre les signatures des solutions anti-bot courantes (en-tetes/HTML de challenge CleanTalk, DataDome, Imperva, hCaptcha...) sur les reponses 403/challenge de la baseline et produire un message dedie ("votre protection anti-bot bloque des clients HTTP legitimes — verifiez la liste d'exemption"), en croisant avec les sondes UA differentielles deja presentes dans `ai-access.ts` pour dire quels bots passent et lesquels sont bloques. Complementaire (pas doublon) de `cloudflare-ai-defaults`, limite a Cloudflare. Impact moyen-fort (transforme un echec d'audit mou en constat vendable, cas reel deja rencontre) / effort moyen (signatures + messages, aucune nouvelle architecture reseau).

## Ajouts 2026-08-25 (session automatique — signaux internes + veille SearXNG)

- [x] A80 [livré 2026-08-25] **Corriger le chemin Windows du store d'usage** — `apps/web/server.mjs:105` : `new URL('./data/', import.meta.url).pathname` renvoie `/C:/...` sur Win32 → `ENOENT mkdir 'C:\C:\...'` répété ~15× à chaque suite de tests, stats d'usage silencieusement perdues sous Windows. Correctif : `fileURLToPath()`. Impact moyen (données perdues + bruit de test) / effort quasi nul.
- **A81 — Release 0.12.0 : rattrapage CHANGELOG + chiffre de checks cohérent** — `packages/cli/package.json` est resté en 0.11.0 alors que 50 commits (dont 14 `feat`, avec nouveaux checks scorés `rsl-license`, `sd-speakable`, RSS) ont suivi la release du 2026-08-02, en contradiction avec la règle « un nouveau check = un minor » du CHANGELOG ; et la description npm annonce 139 checks quand README/README CLI en annoncent 147 (chiffre faux sur la page npmjs publique). Impact fort (crédibilité de l'outil qui prêche la cohérence) / effort faible.
- **A82 — Mode site-wide avec échantillonnage intelligent (inspiration Unlighthouse)** — au-delà de `--max-pages` brut : découverte d'URL via sitemap, échantillonnage par gabarit de page (1 fiche produit ≈ toutes), agrégation site entier avec drill-down par gabarit. C'est le différenciateur mis en avant par Unlighthouse face à LHCI (veille 25/08) et aucun item A1-A79 ne couvre l'échantillonnage. Impact fort (audits de gros sites réalistes) / effort moyen-élevé.
- **A83 — Nettoyer la racine du dépôt** — `extract_lantern.js`, `final_quotes.js`, `pull_strings.js`, `quote_windows.js`, `lantern_*.txt` (68 Ko), `audit-prod.json/.err`, `findable.bordebat.fr-baseline.json` : non trackés et gitignorés mais polluent la racine, et en CommonJS dans un repo `"type": "module"` (non exécutables tels quels). Déplacer dans `tmp/` local ou supprimer. Impact faible / effort quasi nul.
- **A84 — Plan de découpage de `apps/web/server.mjs` (2108 lignes)** — 4× plus gros que le fichier suivant, monolithe routes + rendu + jobs, adossé à 28 fichiers de test (243 tests verts) : extraire progressivement routes/rendu/jobs en modules en s'appuyant sur la suite existante comme filet, avant que le coût de découpage ne croisse encore. Impact moyen (vélocité future) / effort élevé — planifier, pas urgent.
- A85 [2026-08-26] **Distinguer erreur réseau et ressource absente dans le crawler** — `packages/cli/src/crawler.ts` retourne `null` aux :424/:458/:467/:489/:502/:594/:605 et `resolveGuard` :565 `{ok:false}` : timeout, DNS KO, TLS invalide, reset TCP et « ressource inexistante » produisent le même signal. Les checks de ressources optionnelles (llms.txt, `/.well-known/ai.json`, security.txt, RSL) rendent donc un `fail` « absent » sur simple hoquet réseau. Étendre le retour (`{kind:'absent'} | {kind:'network-error', reason}`) et basculer ces checks en `skip` « non vérifiable (erreur réseau) ». Même famille de faux négatifs qu'A79 (403 anti-bots), côté transport. Impact élevé (crédibilité du rapport client) / effort moyen.
- [x] A86 [2026-08-26] (livre 2026-08-26, commit 8894905) **Diff vs baseline dans les sorties CI (JSON/SARIF) + `schemaVersion`** — `index.ts:454-459` appelle `renderSarif`/`renderBadge`/`renderJson`/`renderJunit` sans l'objet `diff` (calculé :391) alors que HTML/Markdown le reçoivent ; en `--json` le diff terminal est aussi supprimé (:421) ; `report/json.ts` est un `JSON.stringify` brut sans version de schéma. Ajouter `diff?: ReportDiff` au JSON, annoter les régressions en SARIF (`error` sur pass→fail), `schemaVersion: 1` lu par la validation de baseline. Veille 2026 : les comparatifs d'outils GEO (Writesonic, plateya.fr « 25 outils ») classent l'intégration CI/monitoring continu parmi les différenciateurs. Impact élevé (usage CI, `--fail-on-regression` muet en format machine sinon) / effort faible-moyen.
- [x] A87 [2026-08-26] (fait 2026-08-26, commit cebaf8c) **Cohérence i18n du terminal en `--lang fr`** — `report/terminal.ts:8-23` force `messages('en')` alors que `index.ts:421` rend le diff localisé : avec `--lang fr --baseline`, verdict et tableau en anglais + section diff en français dans le même stdout. Propager `langTyped` à `renderTerminal` (`FAMILY_LABELS_I18N.fr` existe déjà). Cible TPE/PME francophone en expansion (guides GEO 2026 FR : nicolas-mauhin.fr, aioseo.fr, plateya.fr). Impact moyen (visible à chaque run FR) / effort faible.
- [x] A88 [livré 2026-08-26] **Filets anti-régression : enregistrement des checks + tests de `scoring.ts`** — (a) un test qui importe chaque module de `src/checks/`, détecte les exports de forme `Check` (`id`+`family`+`run`) et vérifie leur présence dans `buildChecks()` : ferme définitivement la classe du bug A74 (check importé mais jamais enregistré), aujourd'hui seul `entity-graph-check.test.ts:66` le fait, pour un seul check ; (b) `test/scoring.test.ts` (somme des `FAMILY_WEIGHTS` == 1.00, bornes `gradeOf` A-F, traitement des `skip`) : le score vendu n'a aucun test unitaire direct. Impact moyen-élevé / effort quasi nul (~30 lignes).
- [x] A89 [2026-08-26] (fait 2026-08-26, commit e269a55) **Codes de sortie CI propres + gate par famille** — `index.ts:560` : le code 2 signifie à la fois « site injoignable » (`UnreachableSiteError`) et « échec d'écriture du rapport » ; réserver 2 à l'injoignabilité, introduire 3 pour l'écriture (l'usage :87 n'annonce que 0/1/2, ajout documentable). Ajouter `--fail-on <family>=<score>` répétable (ex. bloquer sur `ai-access` sans imposer un seuil global dépendant des 8 autres familles). Impact moyen / effort faible.

## Ajouts session automatique 2026-08-26 (phase recherche-ameliorations, cycle 2)

Signaux internes : suites vertes au dernier run trace (1447 tests CLI + 243 web, phase implementation du 26/08), aucun TODO/FIXME ; restes exploratoires `extract_lantern.js`/`final_quotes.js` a la racine (prototypes citabilite jamais industrialises). Verifie par grep avant redaction : "sitemap-ai" 0, "leaderboard" 0, "citation-ready"/"quotable" 0 ; A61 couvre deja la distinction training-bots/citation-bots, A78 la doc llms.txt — non redoubles. Veille externe (SearXNG local 08/2026) : pressonify.ai (audit 11 crawlers IA + `sitemap-ai.xml`), aiindexcheck.com ("citation readiness signals" comme axe d'audit distinct), zerokit.dev (scan des top-100 sites publie en "AI Readiness Leaderboard" — usage marketing direct d'un moteur d'audit), searchscore.io (checklist technique GEO 25 points).

- A90 [2026-08-26] **Check informatif `sitemap-ai.xml`** — convention emergente 2026 (pressonify.ai) : sitemap dedie aux crawlers IA listant les pages a fort signal. Detection + statut informatif (present/absent/referencie dans robots.txt), meme pattern leger que les checks llms.txt/RSL (A71) existants ; ne pas penaliser l'absence tant que l'adoption reste faible, juste informer. | Impact faible-moyen / effort tres faible (pattern de check deja en place, ~1 fichier + tests).
- A91 [2026-08-26] **Leaderboard public "AI Readiness" par vertical** — faire tourner le moteur en lot sur N sites reels d'un secteur (ex. artisans d'Ille-et-Vilaine, synergie directe avec le stock pb-opentech) et publier le classement (page statique + JSON). Double valeur prouvee par zerokit.dev (08/2026) : acquisition/credibilite (contenu citable par les IA elles-memes) ET corpus de test grandeur nature qui debusque des faux positifs du moteur (le dogfooding A60 l'a deja montre a petite echelle : 3 bugs reels). | Impact fort (marketing + robustesse) / effort moyen (le mode batch et le JSON schemaVersion:1 existent deja ; reste l'orchestration + la page).
- A92 [2026-08-26] **Check "citabilite" : donnees chiffrees sourcees et blocs citables** — les moteurs de reponse citent preferentiellement les contenus a statistiques datees, affirmations attribuees et blocs autoportants ("citation readiness", aiindexcheck.com ; confirme par les checklists GEO 2026). Detecter par page : presence de chiffres avec source/date, citations attribuees, paragraphes autosuffisants courts. Industrialiser ce que les prototypes `extract_lantern.js`/`final_quotes.js` de la racine esquissaient deja, puis les supprimer du depot. | Impact moyen-fort (famille contenu, differenciateur vs audits purement techniques) / effort moyen (heuristiques textuelles, pas de LLM requis en v1).

## Ajouts session automatique 2026-08-27 (phase recherche-ameliorations)
Signaux internes : racine du depot polluee par des artefacts de debug (extract_lantern.js, final_quotes.js, pull_strings.js, quote_windows.js, lantern_article.txt, lantern_prose.txt, audit-prod.json/.err, findable.bordebat.fr-baseline.json) ; A86 a livre le diff vs baseline en JSON/SARIF mais rien de lisible client. Signaux externes : SearXNG local (guides GEO TPE/PME 2026 nicolas-mauhin.fr, comparatifs outils GEO plateya.fr/aioseo.fr : les outils payants mettent en avant badges/preuves publiques et rapports de diff lisibles ; IndexNow absent des checks alors que Copilot s appuie sur l index Bing).

- [x] A93 [2026-08-27, livre 2026-08-27 58f2e91] **Check informatif IndexNow** — detecter la presence d une cle IndexNow (fichier {key}.txt a la racine reference par une cle plausible, ou meta equivalente) ; indexation acceleree Bing -> retombees Copilot ; warn informatif jamais fail (meme pattern que rsl-license A71). | Impact moyen / effort faible
- [x] A94 [2026-08-27, constat 2026-08-30 : deja livre par e86f1fd (renderers terminal/md/html i18n) + wiring index.ts:443/492, tests diff.test.ts] **Diff vs baseline lisible humain (rapport HTML + terminal)** — A86 ne couvre que JSON/SARIF (CI) ; ajouter une section "Ce qui a change depuis le dernier audit" (checks passes/regresses, delta de score) dans le rapport HTML et la sortie terminal, i18n EN/FR — c est le livrable recurrent type freelance/agence qui refait l audit chaque mois. | Impact fort / effort moyen
- [x] A95 [2026-08-27, livre 2026-08-30 fcf1072] **Badge SVG "Findable score"** — sous-commande generant un badge SVG statique (score + grade) et un JSON compatible endpoint shields.io, a integrer au rapport ; preuve publique affichable par les sites audites, canal d acquisition organique. | Impact moyen / effort faible
- [x] A96 [2026-08-27, livre 2026-08-27] **Hygiene du depot : artefacts de debug hors racine** — deplacer/ignorer les scripts et sorties ponctuels listes ci-dessus (racine du repo publie sur GitHub) vers un dossier ignore (meme demarche que V87 cote vigie-seo) ; verifier .gitignore et l historique de suivi avant deplacement, aucune suppression de donnees. | Impact faible-moyen / effort faible
- [ ] A97 [2026-08-27, 2e passe] **Nouveau check images (SEO images 2026)** — aucun check dedie aux images dans packages/cli/src/checks (verifie ce jour) alors que Google change le traitement des images en 2026 (source veille blog.zetruc.fr) et que les moteurs IA lisent alt + ImageObject : verifier alt descriptifs, formats modernes (AVIF/WebP), poids, JSON-LD ImageObject/licence, lazy-loading non bloquant. | Impact moyen-fort / effort moyen
- [x] A98 [2026-08-27, livre 2026-08-28] **Support de la directive robots.txt Content-Signals (Cloudflare)** — parser les signaux search / ai-input / ai-train dans ai-access / agent-standards (grep ce jour : aucune occurrence dans les checks), les refleter dans le rapport et verifier la coherence avec AIPREF (A77) ; Cloudflare l injecte sur des millions de sites, un audit findabilite doit la lire. | Impact moyen / effort faible
- [x] A99 [2026-08-27, 2e passe, livre 2026-08-30] **Generateur de sondes vigie-seo depuis les findings (--emit-probes)** — exporter un JSON de sondes suggerees (requetes par faiblesse detectee) consommable par vigie-seo ; pendant inverse du pont V17 (vigie -> remediate findable), ferme la boucle produit audit ponctuel -> surveillance continue et lie commercialement les deux outils. | Impact fort / effort moyen
- [x] A100 [2026-08-28, livre 2026-08-30] **Historique par check (pas seulement par score)** — `history.ts` ne stocke que date + scores de familles (max 500 entrees) : impossible de repondre a « quel check a regresse quand ». Stocker le statut de chaque check par run (compact : id -> statut) et afficher les transitions dans `--history` et le rapport HTML. Complement direct d'A86/A94 pour l'usage agence recurrent. Impact fort / effort moyen.
- [x] A101 [2026-08-28, livre 2026-08-28] **Collision des rapports du meme jour** — `index.ts` ecrit `<host>-<date>.md/.html` : deux audits le meme jour s'ecrasent silencieusement (constate en usage quotidien automatise). Suffixe horaire ou increment `-2` quand le fichier existe, sauf `--report` explicite. Impact moyen (perte de donnees) / effort quasi nul.
- [ ] A102 [2026-08-28] **Analyseur de logs serveur crawlers IA (`findable-audit logs <access.log>`)** — signal externe convergent (veille 28/08 : nelcielo.fr « la metrique que la plupart des equipes ne mesurent pas », seosupernova.fr et nemociment.fr recommandent l'analyse des hits GPTBot/ClaudeBot/PerplexityBot/OAI-SearchBot dans les logs) : nouvelle sous-commande qui parse un access.log et sort part des crawlers IA, pages les plus visitees par eux, 404 sur /llms.txt et /robots.txt, bots bloques. Aucun item A1-A99 ne couvre les logs. Complement naturel du crawl fetch-only (donnee reelle vs heuristique). Impact fort (differenciateur, preuve chiffree pour le client) / effort moyen.
- [ ] A103 [2026-08-28] **Export PDF natif + rapport agrege multi-sites** — le HTML est print-ready mais rien ne produit de PDF ni de vue agregee de plusieurs audits (les concurrents type AI Labs Audit vendent rapports PDF marque blanche + portail client, veille 28/08). Etape 1 : gabarit print + doc « imprimer en PDF » assumee ; etape 2 : `--report agg.html` sur N resultats JSON. Impact moyen / effort faible (etape 1) a moyen (etape 2).
- [ ] A104 [2026-08-28] **Unifier les 4 catalogues i18n** [perimetre corrige 2026-09-01 par A117 : le 4e est `apps/web/lib/i18n.mjs`, 478 lignes, autre workspace et autre langage (mjs vs ts)] — `report/i18n.ts` (types `'en'|'fr'`), `check-i18n.ts` et `message-i18n.ts` (608 lignes) sont paralleles : ajouter une 3e langue (es/de, demande previsible pour un outil publie sur npm) coute 3 fichiers. Fusionner en un format unique par check (title/description/messages) avec fallback en. Impact moyen (extensibilite) / effort moyen.

## Ajouts session automatique 2026-08-29 (phase recherche-ameliorations)

Signal interne majeur : `npm test` ROUGE au 2026-08-29, casse par le commit A98 (`5191b92`) — 3 assertions : le README du paquet CLI annonce 148 checks au lieu de 149 ; 3 gabarits `content-signals` sans traduction FR ; le test d'enregistrement attend 147 checks et en trouve 148. Recidive du pattern « commit auto sans suite verte » (deja vu : tests indexnow oublies de `58f2e91`, rattrapes par `d6a89f0`). Sources externes (SearXNG) : comparatifs GEO 2026 (webconversion.fr, stafe.fr, digitiz.fr).

| # | Idee | Principe | Impact/Effort |
|---|------|----------|---------------|
| A105 | ✅ 2026-08-29 **Reparer la suite cassee par A98 (content-signals)** | mettre a jour le compteur du README CLI (148 → 149), ajouter les traductions FR des 3 messages `content-signals`, ajuster le test d'enregistrement (147 → 148) — CI, action GitHub et dogfooding (P4/P92) restent bloques tant que la suite est rouge | fort / faible |
| A106 | ✅ 2026-08-29 **Compteurs de checks generes depuis le registre** | « annonce N au lieu de N+1 » est la 2e casse du meme type : generer README/guides/badge depuis `buildChecks()` (script de build qui reecrit, plutot qu'un test qui verifie a la main), pour supprimer toute la classe d'erreurs de compteur manuel | fort / faible |
| A107 | ✅ 2026-08-29 **Garde-fou : suite verte obligatoire avant tout commit [auto]** | hook pre-commit ou etape systematique `npm test` avant les commits des sessions automatiques (recidive constatee : `58f2e91` puis `5191b92`) — protege aussi les consommateurs du depot (action GitHub, futur paquet npm) | moyen / faible |
- [ ] A108 [2026-08-30] **Etiquette de confiance par check (impact prouve vs pari raisonnable)** — signal externe (SearXNG 30/08) : la critique publique de llms.txt monte (« llms.txt en 2026 : utile ou inutile ? Le verdict SEO », triaina.fr : « les bots ne scannent pas le web a la recherche de ces fichiers ») alors que robots.txt/schema.org/contenu ont un impact documente. Ajouter une metadonnee `confidence` (prouve / probable / speculatif) par check, affichee dans les rapports HTML + terminal, pour que l'outil reste credible quand un client lit ces articles — et desamorcer l'objection en la devanant. Impact moyen (credibilite produit) / effort faible (metadonnee + rendu, pas de nouvelle logique).
- [ ] A109 [2026-08-30] **Famille de checks E-E-A-T (auteur, dates, sources, a-propos)** — signal externe (SearXNG 30/08) : les checklists d'audit GEO 2026 (victor-lerat.fr « +135 points de controle E-E-A-T et Schema », transacts.fr GEO Score « 7 piliers, 35 criteres ») font de l'E-E-A-T un pilier a part entiere, non couvert par packages/cli/src/checks (verifier l'absence exacte avant implementation) : auteur identifie sur les articles (byline + schema author), dates de publication/mise a jour visibles, liens sortants vers des sources, page a-propos reliee. Impact fort (pilier standard du marche, differenciant en audit local) / effort moyen (3-4 checks HTML sans nouvelle infra).
- [x] A110 [2026-08-30, clos 2026-08-30 sans code : deja couvert par A3/A32 — `topFixes()` (report/recommendations.ts) rend le bloc « Top N actions » en tete du terminal (terminal.ts:43) et du HTML (html.ts:562, section action-plan), trie par points ponderes ÷ cout d'effort (report/effort.ts). Doublon d'analyse phase 2.] **Plan d'action priorise en tete de rapport (top 5 quick wins)** — signal interne : le rapport liste les echecs par famille mais ne repond pas a « par quoi je commence ? » ; signal externe : les outils commerciaux du comparatif GEO 2026 (aioseo.fr, digitiz.fr) vendent tous des « recommandations actionnables » comme differenciateur. Trier les checks en echec par (poids de scoring x effort estime declare par le check) et afficher un bloc « Top 5 actions » en tete des rapports HTML + terminal. Complement lisibilite de A94 (diff baseline) et A103 (agrege). Impact fort (c'est la premiere chose qu'un client TPE lit) / effort faible-moyen.
- [x] A111 [2026-08-31] [FAIT 2026-08-31] **Etendre `sync-check-counts.mjs` aux surfaces publiees (`packages/cli/package.json`, `action.yml`)** — signal interne (session auto 31/08) : le registre compte **149 checks** et `node scripts/sync-check-counts.mjs --check` repond « 0 fichier(s) en decalage », alors que la description npm de `packages/cli/package.json:4` ET la description de l'action GitHub `action.yml:4` annoncent toutes deux **139 checks**. Ce sont les deux seules surfaces vues par un prospect avant d'installer (page npm, Marketplace GitHub), et ce sont precisement les deux que le garde-fou livre par A106 ne couvre pas — le filet a un trou la ou il coute le plus cher. Correctif : ajouter ces deux fichiers a la liste des cibles du script (regex sur `\d+ checks` dans le champ `description`), et couvrir le cas par le hook pre-commit d'A107. Distinct d'A81 (release 0.12.0 / rattrapage CHANGELOG), qui traite la publication, pas l'outil de coherence. Impact fort (credibilite commerciale, chiffre faux publie) / effort tres faible.
- [ ] A112 [2026-08-31] **Parite de rendu Markdown vs HTML** — signal interne (session auto 31/08) : `report/html.ts` fait 663 lignes contre **104 lignes** pour `report/markdown.ts`, et toutes les nouveautes des trois derniers mois (CWV dashboard, plan d'action par couloir d'effort, sparklines `charts.ts` 454 l., diff `diff.ts` 192 l., historique et transitions par check `history.ts` 116 l., dataviz SVG) n'ont atterri que cote HTML. Deux clients demandant « le rapport » recoivent donc deux produits differents selon `--report md` ou `html`, sans que rien ne le signale. Actions : (1) etablir la matrice des sections presentes/absentes par format, (2) porter en Markdown les sections a valeur client (plan d'action, sous-scores par famille, transitions), (3) un test de parite qui echoue quand une section HTML nouvelle n'a pas d'equivalent Markdown ou d'exemption declaree. Prealable naturel a A103 (export PDF) et a A16/A36 (one-pager). Impact fort / effort moyen.
- [ ] A113 [2026-08-31] **Check « declaration d'accessibilite » + verdict reglementaire EAA** — signal externe (SearXNG 31/08, sources officielles et specialisees) : l'European Accessibility Act est applicable en France **depuis le 28/06/2025** ; il vise les entreprises privees des le seuil **10 salaries ou 2 M EUR de CA** qui vendent au grand public (e-commerce, banque, transport, telecom), avec obligation de **publier une declaration d'accessibilite** et sanctions DGCCRF a la cle (platane.io, rgaa-checker.com, aksessia.fr, rgaa-test.fr). La famille `accessibility` de findable-audit mesure aujourd'hui des criteres techniques mais ne dit jamais au client **s'il est en infraction**. Proposition : un check qui cherche la page de declaration d'accessibilite (liens en pied de page, `/accessibilite`, `/declaration-accessibilite`, mentions legales) et verifie la presence des mentions obligatoires (niveau de conformite, date d'audit, moyen de contact/recours) ; plus un encart reglementaire dans le rapport rappelant le seuil et la date. Distinct d'A51 (couverture technique vs criteres RGAA 4.1) : ici c'est le verdict de conformite documentaire, pas la couverture des criteres. Levier commercial direct pour pb-opentech (cf. P108). Impact fort / effort faible-moyen.
- [ ] A114 [2026-08-31] **Affordances agentiques mesurables : catalogue d'API (RFC 9727) et Web Bot Auth** — signal externe (SearXNG 31/08) : Cloudflare a publie en avril 2026 un **« score de preparation aux agents »** dont les criteres explicites sont `robots.txt` / `llms.txt` / **Web Bot Auth**, les Agent Skills, le **catalogue d'API (RFC 9727)** et l'identification des serveurs OAuth (blog.cloudflare.com/fr-fr/agent-readiness/) ; en parallele, edikka.com et le bilan Cloudflare Radar 2025 documentent la bascule de la verification des bots du user-agent falsifiable vers les plages IP publiees et la signature (Web Bot Auth). Concretise l'idee « affordances agentiques » listee sans identifiant en section LOT 6+ : sondes deterministes sur `/.well-known/api-catalog` (RFC 9727) et `/.well-known/oauth-protected-resource`, en informatif d'abord, dans la famille `llm-content` aux cotes d'`agent-usability`. Interet : aligne le score sur le referentiel public d'un acteur majeur, ce qu'aucun outil du comparatif GEO FR ne fait. Impact moyen-fort (differenciation) / effort moyen.
- [x] A115 [2026-08-31] [LIVRE PARTIEL 2026-09-01 : linter + 7 orphelins numerotes A120-A126 ; fusion des 8 groupes de doublons NON faite, elle demande un arbitrage humain] **Assainir le backlog : convention de statut unique et identifiants manquants** — signal interne (session auto 31/08) : le fichier melange **trois formats** (lignes de tableau `| Axx |`, puces `- Axx —`, cases `- [ ] / - [x]`) et **trois marqueurs de livraison** (`[x]`, ✅, `[LIVRE ...]`), ce qui rend tout comptage automatique faux ; **7 items ouverts n'ont aucun identifiant** (l.145-147, l.150-153) plus les 7 idees sans ID de LOT 6+, donc invisibles au dedoublonnage ; et **8 groupes de doublons averes** subsistent (A17≈A102≈l.151 sur les logs IA, A16≈A36≈A103≈l.152 sur le one-pager, A13≈A47, A26≈A49≈A92≈l.150, A43≈A45, A14≈A84, A23≈A35, A6≈A83≈A96). Avec 42 items ouverts sur 110, le tri coute desormais plus cher que la lecture. Action : format unique `- [ ] Axxx [date] **titre**`, numeroter les orphelins, fusionner les 8 groupes en gardant l'ID le plus ancien, et un script `scripts/backlog-lint.mjs` qui refuse un item sans ID ou sans date. Meme constat que V97 (vigie-seo) et P98 (pb-opentech) : c'est un mal commun aux trois projets. Impact moyen / effort faible.

## Ajouts session automatique 2026-09-01 (phase recherche-ameliorations)

Signaux internes : `npm test` relance ce jour a la racine — **exit 0, aucun echec** (dont 243 tests web, 0 skip, 0 todo) ; aucun TODO/FIXME dans `packages/` ni `apps/` ; depot `main` **ahead 2** sur `origin/main` depuis le 31/08 (A111 + backlog, non pousses par consigne). Signaux externes (SearXNG 01/09) : retrait officiel du support des FAQ dans les SERP Google annonce en mai 2026 (journaldunet.com), premier guide officiel Google d'optimisation pour l'IA en mai 2026 (blogdumoderateur.com, natural-net.fr : « aucun schema specifique a l'IA n'est requis, ne pas suroptimiser les donnees structurees »).

- [x] A116 [2026-09-01] [LIVRE 2026-09-01] **Registre date des obsolescences de fonctionnalites (une source, verifiee par test)** — signal interne : la connaissance des retraits est deja la, mais **eparpillee en prose** — un commentaire dans `checks/rich-results.ts:27` (« FAQ, HowTo, Q&A. Google restricted or retired these result types ») et un texte `why` dans `report/check-i18n.ts` (`sd-website-searchaction` : « Google retired the sitelinks search box in November 2024, so this markup no longer buys a search-result feature »). Signal externe : Google a retire le support des FAQ dans les SERP en **mai 2026** (journaldunet.com, 26/05/2026), ce qui confirme que la liste bouge encore. Le produit est deja honnete sur ces deux cas — le probleme n'est pas le fond, c'est que **rien ne garantit qu'un futur check ne revendra pas un gain disparu**, et que la veille doit etre refaite de zero a chaque fois. Un module `deprecations.ts` (identifiant, date, source, checks impactes, ce que la fonctionnalite vaut **encore**) + un test qui impose que tout check touchant un type liste cite le registre. Impact moyen (credibilite : c'est l'argument de vente du rapport) / effort faible.
- [x] A117 [2026-09-01] [LIVRE 2026-09-01 — correction de perimetre reportee dans A104] **Le 4e catalogue i18n, celui du site web, manque a l'inventaire de A104** — signal interne verifie ce jour : A104 [2026-08-28] propose d'unifier **trois** catalogues (`report/i18n.ts` 429 lignes, `report/check-i18n.ts`, `report/message-i18n.ts` 621 lignes), tous dans `packages/cli`. Or `apps/web/lib/i18n.mjs` fait **478 lignes** et constitue un quatrieme catalogue, parallele, dans un autre langage (mjs vs ts) et un autre workspace — c'est-a-dire quatre endroits ou ajouter une langue, et quatre endroits ou une chaine peut manquer. Elargir A104 au perimetre reel plutot que de decouvrir le quatrieme au moment de l'implementer. Impact moyen / effort faible (correction de perimetre d'un item existant).
- [x] A118 [2026-09-01] [LIVRE 2026-09-01 — PREMISSE PARTIELLEMENT FAUSSE] **Revue datee du roster de crawlers** (`Meta-ExternalAgent` n'etait PAS absent : present en minuscules `meta-externalagent` dans `TRAINING_BOTS`, robots.ts:98 — le grep de la phase de recherche s'est trompe. Livre : la moitie valable, `AI_ROSTER_REVIEWED` + `AI_ROSTER_SOURCES` + `test/ai-roster.test.ts`.) — signal interne : le roster est globalement a jour et couvre les arrivees recentes (`Claude-SearchBot`, `Claude-User`, `OAI-SearchBot`, `Google-CloudVertexBot`, `MistralAI-User`, `DuckAssistBot`, `Applebot-Extended`, `Bytespider`, `PanguBot`, `LinerBot`, `Timpibot`…), mais **aucune occurrence de `Meta-ExternalAgent`** (grep insensible a la casse sur `packages/cli/src`, y compris `externalagent`, `Ai2Bot`, `cohere`) alors que c'est l'agent d'entrainement de Meta, documente et parmi les plus frequemment listes dans les robots.txt. Au-dela de l'ajout : le roster n'a **pas de date de derniere revue** ni de source, donc personne ne peut dire s'il est vieux de deux mois ou de six. Ajouter l'agent manquant, dater le roster et le rendre re-verifiable. Impact faible-moyen / effort faible.
- [x] A119 [2026-09-01] [LIVRE 2026-09-01] **`npm test` peut valider les tests web contre un `dist` perime (piege latent, local uniquement)** — signal interne verifie ce jour : `scripts/build-if-missing.mjs` **ne reconstruit que si `dist/` est absent** (`if (existsSync(join(dir, 'dist'))) { skip }`) — aucune comparaison de dates entre `src/*.ts` et `dist/*.js`. Or les tests web importent le build, pas les sources : `apps/web/test/dogfooding.test.mjs` et `dogfooding-ecosystem.test.mjs` font `import { runAudit } from '../../../packages/cli/dist/runner.js'`, idem `compare.test.mjs`. Donc apres une modification des sources CLI sans rebuild, la suite qui a historiquement revele **trois vrais bugs produit** (dogfooding, 26/07) tourne contre l'ancien binaire et reste verte. **Etat du jour : `dist/runner.js` (30/08 21:08) est plus recent que toutes les sources, aucune divergence en cours** — c'est un piege latent, pas une panne. La CI n'est pas concernee (`npm ci` + `npm run build` inconditionnels), ce qui rend le symptome asymetrique et donc trompeur : vert en local, rouge en CI, ou pire l'inverse. Correctif : comparer le `mtime` le plus recent de `src/` a celui de `dist/` et rebatir si la source est plus recente. Impact fort (fiabilite du signal de test) / effort faible.

## Ajouts session automatique 2026-09-01 (phase recherche-ameliorations, 2e passe)

Signaux internes verifies ce jour : depot `main` **ahead 8** sur `origin/main`, arbre propre, aucun TODO/FIXME hors tests ; `git tag` s'arrete a **v0.9.0** ; `CHANGELOG.md` s'arrete a `## 0.11.0 - 2026-08-02` et son dernier commit est `1f73960` (02/08) ; **50 commits** depuis cette date. `report.toolVersion` est bien renseigne (`index.ts:424`) mais **aucune occurrence de `version` dans `report/diff.ts`**. `USAGE` (`index.ts:31`) tient sur **une ligne de 584 caracteres** pour **32 options**. Signaux externes (SearXNG 01/09) : Google a deploye **AI Overviews et AI Mode en France le 22/07/2026** (blogdumoderateur.com 23/07, seo.fr, lafabrique.orange-business.com) et une etude Ahrefs du **17/08/2026 sur 963 domaines** mesure un **recul de 23,1 % du taux de clic** pour les domaines les plus exposes (relayee par plateya.fr).

- [ ] A127 [2026-09-01] **Aucun tag de version depuis v0.9.0, et publication figee depuis un mois** — signal interne : `git tag` liste jusqu'a **v0.9.0** alors que `packages/cli/package.json` annonce **0.11.0** ; **v0.10.0 et v0.11.0 n'ont jamais ete taguees**, alors que `.github/workflows/release.yml` existe. Depuis le dernier passage sur le CHANGELOG (`1f73960`, 02/08), **50 commits** ont atterri : badge SVG (A95), historique par check (A100), diff lisible (A94), generation des compteurs (A106), surfaces npm et action (A111), registre d'obsolescences (A116), roster date (A118), rebuild du `dist` perime (A119). Rien de tout cela n'est visible depuis l'exterieur : qui installe depuis npm ou pointe `uses: piwig/findable-audit@main` recoit un outil dont la derniere trace de publication a un mois, et qui ne peut pas se rattacher a un tag. **Extension d'A81 et non doublon** : A81 traite la coherence du chiffre de checks et le rattrapage du CHANGELOG ; ce qui est constate ici, et qu'A81 ne dit pas, c'est l'**absence de tags** — donc l'impossibilite de dire ce que contient une version installee. Action : taguer retroactivement 0.10.0 et 0.11.0 sur les commits de release correspondants, puis un controle qui refuse une montee de `version` sans tag ni section de CHANGELOG. Impact fort (credibilite d'un depot de candidature publique) / effort faible.
- [ ] A128 [2026-09-01] **`--baseline` compare deux modeles de score sans jamais le dire** — signal interne verifie : le rapport porte bien la version de l'outil qui l'a produit (`report.toolVersion`, `index.ts:424`, commentaire « CLI version that produced this report »), mais **`report/diff.ts` ne contient aucune occurrence de `version`** : le diff se contente d'aligner les scores. Or entre deux versions, des checks sont ajoutes (149 aujourd'hui, +N depuis la derniere publication) et un check de plus dans une famille **dilue** la valeur de chacun des autres, puisque les poids de famille sont fixes et somment a 1.00 (`scoring.ts:4`). Consequence concrete pour le seul usage ou ces options servent — l'agence qui refait l'audit chaque mois et branche `--fail-on-regression` en CI : **une CI cliente peut sortir en 1 parce que notre modele de score a bouge, pas parce que le site a regresse**, et le rapport affirmera « le score a baisse de N points » sans nuance. Action : lire `toolVersion` dans le diff, afficher un avertissement explicite quand la baseline vient d'une autre version, et ne pas declencher `--fail-on-regression` sur une comparaison inter-versions sans `--allow-cross-version`. Prealable naturel a A100 (historique par check) qui souffre du meme biais sur la duree. Impact fort (un faux echec de CI chez un client coute la confiance) / effort faible-moyen.
- [ ] A129 [2026-09-01] **Repere date du deploiement des AI Overviews en France dans le rapport** — signal externe : Google a active **AI Overviews et AI Mode en France le 22/07/2026** (blogdumoderateur.com 23/07/2026, seo.fr, lafabrique.orange-business.com), et Ahrefs a publie le **17/08/2026** une mesure sur **963 domaines** : **-23,1 % de taux de clic** pour les domaines les plus exposes. Signal interne : le rapport parle deja d'IA generative cote outil de mesure (`report/i18n.ts:255/371`, note sur le rapport « Performance IA generative » de la Search Console, avec la limite du bug Google du 13/08) et `checks/ai-access.ts:87` sait qu'AI Overviews passe par Googlebot — mais **rien ne date le basculement du marche francais**. Un client francais qui lit un rapport de findabilite en septembre 2026 a besoin de savoir que la baisse de clics qu'il constate depuis juillet a une cause exterieure documentee, sinon il l'attribue a son site. Ajouter une note datee et sourcee dans le rendu FR, sur le modele exact du registre d'obsolescences A116 (date, source re-ouvrable, ce que cela change **et ce que cela ne change pas**) — **sans toucher au score**, comme A116. Impact moyen (justesse du conseil, desamorcage d'une mauvaise attribution) / effort faible.
- [ ] A130 [2026-09-01] **`--help` : une synopsis de 584 caracteres sur une ligne pour 32 options** — signal interne mesure : `USAGE` (`index.ts:31`) est une ligne unique de **584 caracteres** enumerant l'integralite des options, et `index.ts` declare **32 options** ; dans un terminal de 100 colonnes cela produit six lignes repliees au milieu des mots avant meme la premiere explication. C'est le **premier contact** de quiconque installe le paquet npm ou decouvre le depot, et c'est aussi la surface la plus lue par un evaluateur de candidature. Action : synopsis court (url + les 4 options qui servent a 90 % des usages), puis des sections nommees (audit, comparaison, sorties, CI, reseau) et un `--help <sujet>` pour le detail ; garder le texte long integralement accessible, il est bon — c'est sa mise en forme qui ne l'est pas. Distinct d'A112 (parite Markdown/HTML des rapports), qui traite le livrable et non l'interface. Impact moyen / effort faible.
