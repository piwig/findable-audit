---
name: Bug report
about: A check reports something wrong, or the CLI/web app misbehaves
labels: bug
---

**What happened**

<!-- The command you ran (or the URL you audited on the web app), and what you saw. -->

```bash
npx findable-audit https://… --max-pages 3
```

**What you expected instead**

**Was a specific check wrong?**

<!-- Check id (e.g. `content-lead-answer`), and why the verdict does not match the page.
     A false positive is a real bug here: a check that flags a correct page costs the
     reader more than a check that stays quiet. -->

**Environment**

- `findable-audit` version: <!-- `npx findable-audit --version` -->
- Node version:
- OS:
- Public URL involved (if you can share it):
