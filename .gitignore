node_modules/
dist/
*.tsbuildinfo

# Superpowers visual-companion runtime (brainstorm mockups); docs/superpowers/ stays tracked
/.superpowers/

# graphify knowledge-graph output (local navigation aid, regenerated on demand)
/graphify-out/

# Private admin dashboard + usage-stats data — VPS-only, NEVER committed to the
# public candidature repo (see docs/competitive-analysis-and-roadmap.md §12.A).
# Deployed and maintained by hand on the VPS; the JSONL store stays local there.
/apps/web/admin.local.mjs
/apps/web/test/admin.test.mjs
/apps/web/ADMIN.local.md
/apps/web/data/

# Audit artefacts produced by running the CLI from the repo root while
# dogfooding (reports, baselines, stderr captures). Regenerated on demand;
# docs/research/ holds the write-ups worth keeping.
/audit-*.json
/audit-*.err
/*-baseline.json
/*.md.html

# One-off research/debug scratch, regroupe hors racine dans /scratch/ (A96).
# Disposable: the verified conclusions live in docs/research/.
/scratch/
# Legacy root-level patterns kept so stray regenerated copies stay ignored too.
/lantern_*.txt
/extract_lantern.js
/pull_strings.js
/quote_windows.js
/final_quotes.js

# Worktrees temporaires des agents (isolation de tache) — jamais versionnes.
.claude/worktrees/
